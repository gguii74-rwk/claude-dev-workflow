# dev-cycle 판별 구조 재검토 — 상태 계층 분리 + 단일 우선순위 규칙 (순번 8 · C-10)

- 단계: **7 직접 구현(TDD) 완료 (2026-08-11, GREEN — AC1~AC7 전건 충족, 유효 235런·REFACTOR 5회, 기록 = `.remember/tdd-8-c10.md`) — 다음 = 8 review-loop(impl)** (새 세션, ledger = 이 문서 말미 — C-6 D17). 종결 시 이 줄을 갱신할 것.
- 브리프: `2026-08-07-review-loop-and-pipeline-improvements-brief.md` §실행 순서 순번 8(C-10). **프로그램의 마지막 트랙.**
- 입력: C-6 impl ledger(`2026-08-08-dev-cycle-extension.md` 말미) · 준비 자료 `.remember/prep-8-c10.md`
- 대상: `dev-workflow/skills/dev-cycle/SKILL.md` (0.14.0 기준 95줄) — 판별 절 + 경량 경로 절

## 1. 문제 (실측 근거)

C-6 impl 루프에서 같은 구조 표면의 finding이 반복됐다:

- **계열 A (경량 승인 상태) 5회** — fp-I6·I8·I10·I12·I13. 경로 판정(세션 한정·휘발)과 생략 기록(spec D번호·영속)이 같은 평면에 놓여 경계 문장으로 봉합됐고, 상태 전이마다 구멍이 났다. 사용자 재결정 2회(영속화→세션 한정 전환·기록 존중 경계)를 거쳐 fp-I12·I13은 **ACCEPTED**로 닫혔으며 그 보완 귀착지가 이 트랙이다. 기준선: R-skip4 잔존 이탈 **1/6**(방향은 fail-closed — 배치 232런에서 검증 생략 방향 이탈 0건).
- **계열 B (③ 조건 조합 미포괄) 3회** — fp-I1(plan 있음+ledger 미종결)·fp-I9(5 생략+6 무기록)·fp-I11(4만 생략+5·6 유지). 매 라운드 다른 조합이 열거의 구멍으로 드러났고 국소 수정으로 닫았다. 당시 사용자 판정: 재구성은 범위 초과라 보류, 잔여 리스크로 명시 — "상태 차원이 늘면 또 다른 미포괄 조합이 나온다."
- **인접 4회차** — fp-I14(구현 완료가 4·5·6 무기록 누락을 사면). ① 가드 예외의 일반화로 닫음.

**수렴 관찰**: 위 국소 수정들(fp-I1 조건 추가·fp-I9 자동 해당없음·fp-I11 조건식·fp-I14 일반화)은 전부 "**가장 이른 미완·비생략 단계로 보낸다**"는 암묵 규칙을 조각조각 문면화한 것이다. 이 규칙을 명시 구조로 올리면 미포괄 조합 계열이 정의상 사라진다 — 모든 단계에 완료/해당없음 판정이 있으면 "첫 미완 단계"는 항상 유일하다.

## 2. 목표 / 범위 / 비목표

**목표** — ① 상태 계층(세션 상태 / 문서 기록 / 산출물 / 하한)을 문면에서 명시적으로 분리해 계열 A의 봉합 구조를 해소하고 ② 판별 ③ 조건 열거를 "단계×신호 표 + 첫 미완 단계 규칙"으로 재구성해 계열 B의 구멍 계열을 뿌리에서 제거한다.

**범위** — SKILL.md의 **판별 절(60~86행) + 경량 경로 절(40~50행)**. fp-I14 계열(① 가드 예외) 포함. 나머지 절(파이프라인 표·단계 경계·착수 브리프·하지 말 것)은 최소 접촉(참조 갱신 수준).

**비목표** —
- 기결정 **내용**의 재론: 세션 한정 승인 계약(계열 A 재결정) · 하한 3종(D3) · 단계별 생략+D번호 기록(D2) · 승격 즉시(D4) · fail-closed(D5·D21) · 비소급(D23)은 전부 유지한다. 이 트랙은 이 결정들의 **구조·표현**만 다룬다.
- description·트리거 변경(G-c/G-e — 발견성 재검증 TDD를 부르므로 건드리지 않는다).
- 새 기계 장치(파서·스크립트 게이트) 도입 — G-b 유지.
- V1 실측(경량 경로 우회 감소 여부) 재조사 — 0.11.0 배포 후 관찰 창이 1일이라 데이터가 없다. 후속 관찰로 유지.

## 3. 설계

### 3a. 경량 경로 절 — 상태 계층 정의

절 앞머리에 계층 표를 신설하고, 기존 규칙을 내용 그대로 각 계층 아래로 재배치한다:

| 계층 | 내용 | 수명 | 판별이 다루는 법 |
|---|---|---|---|
| 세션 상태 | 경량/정식 판정·승인 | `/clear`에서 소멸 | 추정 금지, **재확인**(미확인 기본값 = 정식) |
| 문서 기록 | 생략 기록(D번호+근거) · 철회 표시(기존 생략 기록에 부기) · 승격 사유(spec에 기록 — **D번호 불요**, D4 계약 그대로) | 영속 | **존중** — 재확인 대상 아님. 철회분 불인정 · 활성 승격 사유와 미철회 생략 기록의 충돌은 가드 1로(아래 충돌 규칙) |
| 산출물 | spec·plan·ledger 존재/종결 | 영속 | 단계 상태 판독 신호 |
| 하한 3종 | spec 문서·3 harden-spec·8 impl 루프 | 불변 | 기록으로 덮지 못함(생략 기록 무효) |

- 기존 44행의 판정 규칙(지도 1차 + 확인 1회, 승인 시만 경량, 거절·무응답·모호 = 정식)은 '세션 상태' 아래로 이동, 내용 불변.
- fp-I13의 봉합 문장("재확인 대상은 아직 수행하지 않은 단계…")은 **삭제하고 계층 정의의 귀결로 대체** — 재확인은 세션 상태에만 적용되고, 문서 기록은 정의상 재확인 대상이 아니다.
- 승격(D4)·생략 기록(D2)·하한(D3) 항은 각자 계층 아래 재배치, 내용 불변.
- **문서 기록 계층 내 충돌 규칙(C-10 D17)**: 활성 승격 사유 기록과 **미철회 생략 기록**이 공존하면 그 공존 자체가 **불명확 신호**다 — 그 생략이 승격 전 정상 종료된 것인지 철회 누락인지는 기록만으로 판독되지 않는다(D4 승격 기록은 사유만 요구). 따라서 **가드 1(fail-closed)로 보낸다**: 통과시키지 않고 사용자 1회 확인, 확인 전 기본값은 정식(그 단계 미완 취급). D4의 기록 계약은 불변이고, 기록이 명확한 경우(철회 표시 실재)는 충돌이 아니므로 질문 없이 판독한다. (fp-S3·S5·S6 계열 종결 — R4 ESCALATE 사용자 판정)

### 3b. 판별 절 — 단일 규칙 + 단계×신호 표

열거 7항(③)과 ①·② 절차 서술을 다음으로 대체한다:

> **판별 규칙**: 1→9 순서로 각 단계의 상태를 판독해 **첫 미완 단계 하나만** 안내한다(C-10 D16 — 별도 안내 원칙 문장을 두지 않는다). 단계 상태 = **완료**(산출물 신호) ∨ **해당없음**(행별 비대상 조건 · 선행 단계의 자동 귀결(예: 5 해당없음 → 6 자동) · 유효한 문서 기록 — 철회분 불인정) ∨ **미완**. 기록 없는 비대상·자동 귀결은 합법 경로다(별도 D기록 불요 — fp-I9). 신호는 문서 조회(grep 수준)로 본다.

**단계×신호 표 (문면 초안 — 구현에서 최종 문구 확정 · 형식 = markdown 표, C-10 D11)**. 파일럿 게이트(C-10 D11): arm당 1런 파일럿에서 표 오판독 기인 이탈이 관측되면 열거형 불릿으로 폴백한다.

| 단계 | 완료 신호 | 해당없음 조건 | 미완 시 안내 |
|---|---|---|---|
| 1–2 | `docs/specs/<feature>` 존재 + 6항목 골격(부분 결손은 3행 소관 — C-10 D14) | **없음(하한 — spec 문서)** | brainstorming (+착수 브리프) |
| 3 | 재논의 금지 블록 + D번호 결정·근거 실재 + 6항목 내용 기준 충족(D21) | **없음(하한)** | harden-spec |
| 3.5 | spec `## UI 설계` 섹션 + 목업 경로 실재 | 비대상(화면 무관 또는 ui-mockup 트리거 하한 미달 — 문구·색·아이콘·단일 컨트롤 수준 경미 변경) 또는 유효 생략 기록(D번호+근거 — D2 형식) | ui-mockup |
| 4 | spec ledger 종결 | 유효 D번호 생략 기록 | review-loop(spec) |
| 5 | plan 존재 | 유효 D번호 생략 기록 | writing-plans-split |
| 6 | plan ledger 종결 | **5가 해당없음이면 자동**, 또는 유효 기록 | review-loop(plan) |
| 7 | 구현 완료 | — | plan 있으면 SDD, 5 해당없음이면 직접 구현(TDD) |
| 8 | impl ledger 종결 — 위치 = plan 말미 · planless는 spec 말미(진행 중 승격으로 plan이 생겨도 spec 말미 유지 · 규정 도입 전 트랙의 비표준 위치 인정 = review-loop 위치 계약) | **없음(하한)** | review-loop(impl) |
| 9 | PR merged + repo 규약 후속 | — (경로 무관 공통) | finishing + repo 규약. 완료면 **"트랙 완료"** |

- spec 게이트 6항목의 **내용 기준** 확인(D21)은 1–2행(골격)·3행(내용 완전성)의 완료 신호로 **분담**(표제 일치 아님).
- 6항목 부분 결손 분기(C-10 D14): spec 파일 부재·골격 부재(대부분 항목이 빈 껍데기) → **1–2행 미완**, 파일 존재 + 일부 항목 내용 결손 → **3행 미완**(harden-spec이 질문으로 메움) — 위 1–2·3행 완료 신호에 직접 encode: 1–2 완료 = 골격 기준이므로 첫 미완 규칙이 부분 결손을 1–2로 보내지 않고, 3행이 내용 완전성으로 받는다(현행 "1–2 또는 3"의 기준 미정의 해소).
- 하한 행의 해당없음 열 부재가 곧 ①의 하한 가드다. fp-I10ⓐ·fp-I14의 "첫 미완이 8보다 먼저" 예외는 규칙 자체가 되어 소멸한다.

**규칙 밖 가드 3종** (표 판독의 전제, 별도 문단 유지):
1. **fail-closed** — 신호가 불명확하면(6항목 내용 불명·harden 실행 불명·repo 밖 사실) 통과시키지 않고 사용자 1회 확인.
2. **비소급(결과 기준 — C-10 D10)** — 되돌림이 **종전(0.10.0) 판별과 다른 결과**에 기인할 때만 legacy 확인 1회(기존 트랙이면 종전 판별). 의미 불변 이식 지점은 종전 규칙 취급(비소급 불요) — 규칙명 열거 방식은 폐기한다. 8은 종전 판별에도 있으므로 비소급이 8을 건너뛰는 근거가 되지 않는다.
3. **세션 상태 재확인(C-10 D13)** — 세션 상태가 판별 결과를 가를 때만(트랙이 경량 운영을 시사·명시하는데 미기록 생략 결정이 걸릴 때) 그 세션 1회 확인. 그 외에는 질문 없이 기본 정식(3a 계층 규칙 · C-P arm 기대 동작).

### 3c. 기결정 이식 표 (spec 원본 — spec 루프의 감사 대상)

원칙: **각 결정은 새 문면의 정확히 한 곳에 산다**(거울면 금지 — 7c 교훈). 이식 누락·의미 변형은 4단계 spec 적대검증의 1차 감사 대상이다.

| 원 결정/수정 | 요지 | 새 거처 |
|---|---|---|
| D1·D2 | 분기 축=접촉 표면 · 생략은 단계별+D기록 | 경량 절 서두(불변) + '문서 기록' 계층 |
| D3 | 하한 3종 불가침 | '하한' 계층 + 표의 해당없음 열 부재 |
| D4 | 승격 즉시 + 생략 기록 철회 | '문서 기록' 계층(철회 불인정은 표 판독 규칙) |
| D5 | 판정 = 확인 1회, fail-closed | '세션 상태' 계층 + 가드 1 |
| 계열 A 재결정 | 승인 세션 한정, 미확인 기본 정식 | '세션 상태' 계층 수명 정의 |
| fp-I13 수정 | 재확인 대상 경계 | 계층 정의의 귀결(봉합 문장 삭제) |
| D20 | 생략 기록을 부재 판정보다 먼저 | 단계 상태 판독 순서(완료→해당없음→미완) |
| D21 | 하한 fail-closed · 6항목 내용 기준 · 구현완료+ledger미종결→8 | 하한 행 구조 + 가드 1 + 1–2·3행 주석 + 8행 |
| D23·fp-I5 | 비소급 + ③ 되돌림 포함 | 가드 2 — C-10 D10 결과 기준으로 재정의(의미 불변 이식은 종전 규칙 취급) |
| fp-I1 | plan ledger 미종결 → 6 | 6행 완료 신호 |
| fp-I9 | 5 생략 시 6 자동 해당없음 | 6행 해당없음 조건 |
| fp-I11 | ③-5 조건식 | 4·5행 독립 판독으로 분해(조건식 소멸) |
| fp-I10ⓐ·fp-I14 | 무기록·철회 4·5·6이 8보다 먼저 | 판별 규칙 자체("첫 미완 단계") |
| fp-I4 | 트랙 완료 분기 | 9행 완료 신호 + "트랙 완료" |
| D16·3.5 규칙 | 3.5는 경량 자동 생략 아님 · 직후는 항상 4 | 3.5행 + 순서 규칙(3.5 다음 행이 4) |
| D17·review-loop 위치 계약 | planless impl ledger = spec 말미 · 진행 중 승격 시 이동 금지 · legacy 비표준 위치 인정 | 8행 완료 신호 괄호 |
| G-g·G-b | 읽기 전용 · grep 수준 신호 | 불변 제약(하지 말 것 절 유지) |

### 3d. 문면 제약

- G-b: 신호는 grep 수준, 파서·기계장치 금지. 표는 사람·LLM이 읽는 문면이지 실행물이 아니다.
- G-g: 읽기 전용 지도 유지. 착수 브리프 화면 출력 규칙 불변.
- 줄 수(C-10 D15): **하드 상한 100줄**(C-6 D15 승계) · 95줄 내외는 소프트 목표(열거 7항+예외 문장들의 표 압축분 ≈ 계층 정의 추가분). 초과 시 압축 순서 = 괄호 부연·예시 → 계층 표가 흡수한 중복 서술 — 이식 표(3c)의 의미 보존 항목은 압축 대상이 아니다.

## 4. Acceptance Criteria

- **AC1** 검증 *생략* 방향 이탈 **0** (TDD 배치 전체 — 절대선).
- **AC2** R-skip4 계열(기록된 생략 + `/clear` 재개) 이탈률이 기준선 **1/6 미만**(최소 6런, 동률은 미달). 잔존은 fail-closed 방향만 허용. **미달 시 처분 = C-10 D9**(예산 내 반복 후에도 미달이면 릴리스 중단 + 사용자 에스컬레이션 — 완화 경로 없음).
- **AC3** C-6 회귀 arm 전건 무회귀 (C-P·P2·P3·S5·S5n·S6·S7·N2·E3·M4·M5·P4 + 경량 판정 축 = C-6 §6의 경량 판정·하한 준수 시나리오군).
- **AC4** 신규 조합 arm 2종(C-10 D12 — ① 4·6 생략+5 유지 ② 3.5 완료+4 생략 기록→5) + **C-10 D14 분기 arm 2종**(골격 부재→1–2 · 일부 결손→3) + **승격·위치 계열 arm 3종**(① 승격 사유 기록[D번호 없는 사유-만 형식 포함 — fp-S9]+미철회 생략 기록 공존[철회 누락이든 정상 닫힘이든 동일 정적 상태] → 가드 1 확인 발동·무단 통과와 무단 회귀 모두 없음(C-10 D17) ② planless→승격→plan 생성 → impl ledger spec 말미로 8 완료 판독 ③ legacy 비표준 위치 ledger → 인정·8 재권고 없음) + **3.5 계열 arm 2종**(① 경미 UI 변경(트리거 하한 미달) → 별도 D기록 없이 3.5 비대상 통과·미완 회귀 없음 ② 근거 없는 임의 생략 문구 → 무효·3.5 미완) + **6 자동 해당없음 arm 1종**(5 유효 생략 → 6 자동 해당없음, D기록 없이 7로 — fp-S8 검증) 전건 정답 일치 + 되돌림 시 비소급 확인(C-10 D10 결과 기준) 경유. **각 arm(시나리오)은 파일럿 1런을 제외한 본배치 독립 5런 이상**(브리프 §0-2 시나리오별 5+ reps 의무 — 대조군 포함), 이 하한 미달 상태로는 GREEN 종결 불가(fp-S10).
- **AC5** 이식 표(3c) 전 항목이 새 문면에 거처를 갖고 거울면 0 — 4단계 spec 루프가 표로 감사.
- **AC6** G-g 유지(판별 rep의 파일 생성·수정 0건) · G-b 유지(기계장치 0).
- **AC7** `none` 대조군으로 개정 고유 기여를 변이 제거로 서술(보고 요건 — 게이트 아님).

## 5. TDD 계획 (writing-skills — 브리프 §0-2 필수)

- 하네스: C-6 `dcx` 스크래치패드 재사용(2026-08-10 생존 확인). 소실 시 `.remember/remember.md` §하네스의 재생성 명령.
- 승계 관례 전면 적용: baseline은 `git show HEAD:`에서 추출 · 픽스처 정합성 스크립트 검사(checkfix 계열) · **본배치 전 arm당 1런 파일럿** · 호출자/에이전트 문서 물리 분리 · 픽스처에 합격 기준 노출 금지 · fan-out 리포트 파일화(3줄 요약 반환).
- arm 3축: **회귀**(AC3 목록 재실행) / **신규**(C-10 D12 확정 — ① 4·6 생략+5 유지 ② 3.5 완료+4 생략 기록→5 + C-10 D14 분기 arm 2종(골격 부재→1–2 · 일부 결손→3) + 승격·위치 계열 arm 3종 + 3.5 계열 arm 2종 + 6 자동 해당없음 arm 1종(AC4 목록), R-skip4 재현은 AC2 측정용 별도) / **대조**(`ARM=none`). 파일럿(arm당 1런)은 표 형식 판독 게이트를 겸한다(C-10 D11). **본배치 = 시나리오당 독립 5런 이상**(AC4 하한 — 브리프 §0-2).
- 예산: 백 런대. 주간 한도 재설정(월 08:59) 직후가 최적. 부족 시 하한·경량 판정 축 우선 분할.

## 6. 트랙 운영 (자기적용)

- **경량 진입** — 접촉 표면: 스킬 문서 1개 + spec 문서(실행 코드·권한·데이터 없음). 사용자 승인 2026-08-10 브레인스토밍 세션 + **harden 세션 재확인**(세션 한정 규약 이행). 영속 생략 기록 = **C-10 D6**.
- **5 plan·6 plan 루프 생략(C-10 D6)** — C-6 D19 전례. **4 spec 적대검증은 정식 실행**(구조 변경 트랙이라 이식 감사가 필요).
- impl ledger 위치 = 이 spec 문서 말미(C-6 D17). 루프 상태 파일 = `.remember/loop-2026-08-10-dev-cycle-state-priority-<phase>.md`(0.14.0 규약).
- 경로: 3 harden-spec → 4 review-loop(spec) → 7 직접 구현(TDD) → 8 review-loop(impl) → 9 릴리스(0.15.0).

## 7. 완료 조건

- 트랙: SKILL.md 개정 + TDD GREEN(AC1~AC4·AC6) + spec 루프 감사 통과(AC5) + `plugin.json` 0.15.0 + README 3종 동기 + 3머신 `/plugin update` 안내.
- **프로그램 종결**(이 트랙이 마지막): 종료 보고에 ops-hub 실측 대조 항목 — 확인 부채 발생률 · 재지목 건수 · outcome 채움률 — 을 후속 관찰 지표로 명시(브리프 §완료 기준).

## 8. 브레인스토밍 확정 사항 (2026-08-10 — harden-spec에서 C-10 D1~D8로 승격 완료, §재논의 금지 참조)

| # | 결정 | 근거 |
|---|---|---|
| B1 | 기결정 경계 = **구조·표현만**, 내용 재론 금지 | 계열 A 사용자 2회 판정 존중 |
| B2 | 표면 = 판별 절 + 경량 경로 절 전체, fp-I14 편입 | 계열 A 실제 표면이 ③ 밖 |
| B3 | AC = **방향 기준**(생략 방향 0 절대선 + fail-closed 잔존은 개선 조건부 허용) | fp-I13 ACCEPTED 논리 일관 + LLM 변이 견고성 |
| B4 | 접근 = 상태 계층 + 단일 우선순위 규칙 + 단계×신호 표 | 국소 수정 8건의 수렴 관찰. 열거 유지안(A)·유예안(C) 기각 |
| B5 | 기결정 이식 표 필수, 거처 유일(거울면 금지) | 7c 수정 전파 교훈 |
| B6 | 이 트랙 = 경량, 5·6 생략, 4 정식, impl ledger = spec 말미 | C-6 D19 전례 |
| B7 | V1 재조사 생략 | 관찰 창 1일, 데이터 없음 |
| B8 | 파일명 `2026-08-10-dev-cycle-state-priority.md`, 릴리스 0.15.0 | repo 명명 규약 |

## 9. 미해결 질문 (harden-spec 2026-08-10 전건 해소 — 현재 열린 질문 0건)

1. **표 형식의 판독 견고성** — 단계×신호 표를 마크다운 표로 둘지, 열거형 불릿(현행 스타일)로 둘지. → **해소(C-10 D11)**: 표로 초안 + 파일럿 게이트, 오판독 기인 이탈 관측 시 불릿 폴백.
2. **신규 조합 arm 선정** — 계열 B 잔여 조합 후보 중 어느 2~3개가 판별력이 높은가. → **해소(C-10 D12)**: ① 4·6 생략+5 유지 ② 3.5 완료+4 생략 기록→5.
3. **비소급 가드의 재정의** — 의미 불변 이식 지점은 종전 규칙 취급(비소급 불요)으로 두는 문면이 필요한가. → **해소(C-10 D10)**: 결과 기준 재정의 — 종전 판별과 다른 결과를 내는 되돌림에만 발동.
4. **줄 수 예산 초과 시 우선순위** — 95줄을 넘기면 무엇을 압축하나. → **해소(C-10 D15)**: 하드 100·소프트 95, 부연·예시 → 중복 서술 순, 이식 항목 압축 불가.
5. **판별 결과의 안내 문구** — "해당 단계만 안내" 원칙의 문구가 자연스러운가. → **해소(C-10 D16)**: 판별 규칙 문장에 "첫 미완 단계 하나만 안내"로 내장.

## 재논의 금지(기결정)

> 표기: 접두어 없는 D<n>·fp-* 표기는 **C-6**(`2026-08-08-dev-cycle-extension.md`)의 기결정·ledger 항목을 가리키고, **이 트랙 자신의 결정은 본문 어디서든 `C-10 D<n>`으로 명시**한다. 아래 표의 D<n>은 표두에 밝혔듯 전부 C-10의 결정이다.

**승계 기결정 (재론 금지)** — C-6 **G-a~G-j** 전부(특히 G-b 기계장치 금지 · G-d no-AI-trace · G-e 스킬 문면 TDD 필수 · G-f 릴리스 규약 · G-g 읽기 전용 지도 · G-h 단계 경계 = 새 세션), C-6 **D1~D23**(특히 D2 단계별 생략+D기록 · D3 하한 3종 · D4 승격 즉시 · D5 판정 계약 · D15 100줄 상한 · D16 3.5 규칙 · D17 planless ledger 위치 · D19 자기적용 전례 · D20 생략 기록 우선 · D21 하한 가드 · D23 비소급), **계열 A 재결정**(승인 세션 한정 · 미확인 기본 정식 — 사용자 2회 판정), **fp-I13 사용자 판정**(기록 존중 경계), **fp-I12·fp-I13 ACCEPTED**(보완 귀착지 = 이 트랙). 이 트랙은 이 결정들의 **구조·표현만** 다룬다(C-10 D1).

**이번 harden-spec에서 확정 (2026-08-10 · C-10 D1~D16)** — C-10 D1~D8은 브레인스토밍 확정(§8 B1~B8)의 승격, C-10 D9~D16은 harden 압박 질문에서 사용자 결정. **C-10 D17은 spec 적대검증 루프 R4의 ESCALATE 사용자 판정**(2026-08-10). 원본 설계는 §3.

| # | 결정 |
|---|---|
| D1 | 기결정 경계 = **구조·표현만**, 내용 재론 금지 (←B1) |
| D2 | 재검토 표면 = 판별 절 + 경량 경로 절 전체, fp-I14 편입 (←B2) |
| D3 | AC = **방향 기준** — 검증 생략 방향 이탈 0 절대선, fail-closed 방향 잔존은 개선 조건부 허용 (←B3) |
| D4 | 접근 = 상태 계층 + 단일 우선순위 규칙("첫 미완 단계") + 단계×신호 표. 열거 유지안·유예안 기각 (←B4) |
| D5 | 기결정 이식 표(§3c) 필수, 각 결정의 거처 유일(거울면 금지) (←B5) |
| D6 | **트랙 운영 = 경량. 4 spec 적대검증 정식 실행, 5 plan·6 plan 적대검증 생략 — 이 항이 그 생략 기록이다. impl ledger = spec 말미.** 경량 승인 = 브레인스토밍 세션(2026-08-10) + harden 세션(2026-08-10) 재확인 (←B6) |
| D7 | V1 실측 재조사 생략 — 관찰 창 1일, 데이터 없음. 후속 관찰 유지 (←B7) |
| D8 | 파일명 유지, 릴리스 = **0.15.0** minor (←B8) |
| D9 | **AC2 실패 경로** = 예산 내 문구 반복 후에도 미달이면 릴리스 중단 + 사용자 에스컬레이션(기본 후보 = 재구성 폐기·현행 문면 유지). AC2는 하드 게이트 — 동률 허용 등 완화 경로 없음 |
| D10 | **비소급 가드 = 결과 기준** — 종전(0.10.0) 판별과 다른 결과를 내는 되돌림에만 legacy 확인 1회. 의미 불변 이식 지점은 종전 규칙 취급(비소급 불요). 규칙명 열거 방식 폐기 |
| D11 | 신호 표 형식 = **markdown 표 + 파일럿 게이트** — arm당 1런 파일럿에서 표 오판독 기인 이탈 관측 시 열거형 불릿 폴백 |
| D12 | 신규 조합 arm 2종 = ① **4·6 생략+5 유지** ② **3.5 완료+4 생략 기록**(정답 = 5 — 구 "3.5 직후 항상 4" 문장과 갈리는 의미 변경 지점, C-10 D10 결과 기준 검증 겸용) |
| D13 | **세션 상태 재확인(가드 3) 발동 = 판별 결과를 가를 때만** — 트랙이 경량 운영을 시사·명시하는데 미기록 생략 결정이 걸릴 때 그 세션 1회. 그 외 질문 없이 기본 정식(C-P arm 기대 동작 일치) |
| D14 | **6항목 부분 결손 분기** = 파일 부재·골격 부재 → 1–2, 파일 존재+일부 항목 내용 결손 → 3(harden이 질문으로 메움). 1–2·3행 완료 신호 주석으로 encode |
| D15 | 줄 수 = **하드 상한 100줄**(C-6 D15 승계) · 95줄 내외 소프트 목표. 초과 시 압축 순서 = 괄호 부연·예시 → 계층 표가 흡수한 중복 서술. 이식 표 의미 보존 항목은 압축 불가 |
| D16 | "해당 단계만 안내" 원칙 = 판별 규칙 문장에 **"첫 미완 단계 하나만 안내"로 내장**, 별도 원칙 문장 삭제 |
| D17 | **충돌 판독 = 가드 1 일치** — 활성 승격 사유와 미철회 생략 기록의 공존은 불명확 신호로 취급, 통과 없이 사용자 확인 1회(기본 정식). D4 기록 계약 불변. fp-S3·S5·S6 계열 종결 (spec 루프 R4 ESCALATE 사용자 판정 2026-08-10) |

## 잔여 리스크 (DEFERRED)

**DEFERRED(사용자 유예): 0건** — harden-spec 판단 갭 8건(고위험 단독 3 + 묶음 5)이 전부 질문으로 닫혔다(C-10 D6·D9~D16). §9 미해결 질문 5건도 전건 해소. 사실 갭 5건은 조회로 해소(타 스킬 교차 참조는 review-loop impl ledger 위치 계약 1건 — 8행 완료 신호에 이식(fp-S4) · README 3종은 의미 요약뿐이라 의미 불변 개정에서 내용 변경 비강제 · AC3 arm 명칭 전부 C-6에 실재 · C-6 D15=100줄 상한 실재 · dcx 하네스 생존).

**검증 필요(실측) — 구현·종결 단계 항목**

| # | 항목 | 언제 측정 |
|---|---|---|
| V1 | (C-6 V1 승계) 경량 경로의 우회 감소 실효 — 이 트랙 비목표(C-10 D7) | 0.15.0 이후 ops-hub 재조사 |
| V2 | 프로그램 종결 보고의 ops-hub 실측 대조 3항목 — 확인 부채 발생률 · 재지목 건수 · outcome 채움률 | 순번 8 종료 보고(§7) |

## 적대검증 ledger (spec)

- 루프: `review-loop --phase spec` · base = main@`08866bc8d2f30d7b3d7a472cb53fdb113e5d6f1a` · 예산 max 5 / confirm 2 / auto 3
- score 산식: Σ weight(§2c 분류 직후·수정 전 미판정 blocking), weight = critical 4 · high 3 · medium 1 · low 0. 미확인 FIXED 큐 제외.

| fingerprint | 발생 | severity | disposition | 근거·수정 |
|---|---|---|---|---|
| fp-S1 · spec §3b · 첫 미완 규칙이 6항목 부분 결손을 1–2로 오배정(표 초안이 C-10 D14 분기를 구현 불능으로 만듦) · rec: 1–2=골격/3=내용 완전성 분담+검증 arm | R1 | medium | FIXED(R1) | 표 1–2·3행 완료 신호를 C-10 D14 분기로 분담 재정의 + C-10 D14 분기 arm 2런을 §5·AC4에 추가. 커밋 `bb5f2eb` |
| fp-S2 · spec 전반 · C-10 결정 참조가 표기 선언상 C-6 네임스페이스로 해석(C-10 D6·D9~D16 충돌) · rec: C-10 D<n> 전수 명시 | R1 | medium | FIXED(R1) | 본문·재논의 블록 산문의 C-10 결정 참조 전수에 `C-10` 접두 부여 + 표기 선언 개정. 커밋 `bb5f2eb` |
| fp-S3 · spec §3a · 활성 승격 사유 기록+미철회 생략 기록 공존 시 fail-open(생략 기록 존중이 정식 검증 우회) · rec: 충돌 시 생략 기록 무효화 또는 fail-closed + arm 추가 | R2 | high | FIXED(R2) | 문서 기록 계층에 충돌 규칙 신설(미철회 생략 기록 무효 → 해당 단계 미완·정식, D4 철회 의무의 판독측 완성) + 승격·위치 arm ①. 커밋 `f475a79` |
| fp-S4 · spec §3b 8행·§3c D17행 · impl ledger 위치가 review-loop 계약(진행 중 승격 시 spec 말미 유지·legacy 위치 인정) 누락 · rec: 8행에 계약 이식 + arm 추가 | R2 | medium | FIXED(R2) | 8행 완료 신호에 위치 계약 명시 + 3c D17행 확장 + 잔여 리스크의 "타 스킬 교차 참조 없음" 정정 + 승격·위치 arm ②③. 커밋 `f475a79` |
| fp-S5 · spec §3a 충돌 규칙 · R2 신설 충돌 규칙이 승격 전 정상 종료된 과거 생략 기록까지 무효화(닫힌 단계 회귀 유발 — fp-S3와 별개 결함) · rec: 무효화 대상을 승격 시점 미수행 단계로 한정 + 음성 arm | R3 | medium | FIXED(R3) | 무효화 경계를 D4 철회 의무 범위와 동일하게 한정(승격 전 닫힌 기록 존중 명시) + 음성 arm ④(4 생략 완료 후 7 승격 → 4 재권고 없음). 커밋 `86da96f` — **R4에서 C-10 D17로 대체됨** |
| fp-S6 · spec §3a 충돌 규칙 · R3 시점 경계를 정적 기록만으로 판독할 신호가 없음(D4 기록은 사유만 요구) + "질문 없음" 단정이 가드 1과 모순 · rec: 기록 계약 확장 또는 가드 1 일치 | R4 | high | **ESCALATE(R4 즉시) → 사용자 판정 → FIXED(R4)** | fp-S3·S5·S6 동일 표면 3회째 — 2회 반복 규칙 발동으로 사용자 설계 판정. **사용자 선택 = 가드 1 일치(C-10 D17 등재)**: 충돌 공존 = 불명확 신호 → 통과 없이 확인 1회·기본 정식, D4 기록 계약 불변. 충돌 규칙 재작성 + 승격 arm ①·④를 단일 충돌 arm으로 통합. 커밋 `6ac3e48` |
| fp-S7 · spec §3b 3.5행 · 해당없음 조건이 ui-mockup 계약 축약(경미 변경 트리거 하한 미달 자동 통과·유효 생략 형식 소실) · rec: 하한선·유효 형식 명시 + arm | R4 | medium | FIXED(R4) | 3.5행 해당없음 조건에 트리거 하한(문구·색·아이콘·단일 컨트롤)·유효 생략 형식(D번호+근거) 명시 + 3.5 계열 arm 2런 추가. 커밋 `6ac3e48` |
| fp-S8 · spec §3b 판별 규칙 · 전역 해당없음 정의(유효 문서 기록만)가 표의 기록 없는 합법 경로(3.5 비대상·5 생략 시 6 자동)와 모순 — 오배정·교착 유발 · rec: 3원 정의로 일치 + 무기록 진행 arm | R5 | medium | FIXED(R5) | 해당없음 = 행별 비대상 · 선행 단계 자동 귀결 · 유효 문서 기록 3원으로 재정의(fp-I9 구현) + 3.5 arm ①에 무기록 명시 + 6 자동 arm 1런 추가. 커밋 `21042a3` |
| fp-S9 · spec §3a 계층 표 · 문서 기록 행이 승격 사유까지 "(spec D번호)" 형식으로 묶어 D4 계약(사유만 요구)과 모순 — 비-D번호 승격 기록이 무효 판독되면 C-10 D17 가드 1 발동 불능(생략 기록 재통과) · rec: 형식 분리 + 비-D번호 승격 arm | R6(복귀) | high | FIXED(R6) | 문서 기록 행을 생략(D번호+근거)·철회(부기)·승격 사유(D번호 불요, D4 계약 그대로)로 형식 분리 + 승격 arm ①에 사유-만 형식 명시. 커밋 `6d1cf27` |
| fp-S10 · spec §4 AC4·§5 · 신규 상태 분기가 시나리오당 1런 열거뿐 — 브리프 §0-2의 시나리오별 5+ reps 의무가 게이트화 안 돼 우연한 1회 정답으로 GREEN 가능 · rec: 본배치 5런 하한 명시 | R6(복귀) | medium | FIXED(R6) | AC4에 "각 arm 파일럿 제외 본배치 독립 5런 이상(대조군 포함)·하한 미달 GREEN 종결 불가" 게이트 + §5에 본배치 하한 명시, arm 명칭 런→종 정정. 커밋 `6d1cf27` |

- **score 이력**: R1 = 2 (medium 2, 전부 신규) → R2 = 4 (high 1·medium 1, 전부 신규 — 비감소 1회째) → R3 = 1 (medium 1, 신규 — 감소) → R4 = 4 (high 1·medium 1, 전부 신규) → R5 = 1 (medium 1, 신규 — 감소) → C1 지목 = 1 (fp-S2 잔존 medium) → R6(복귀) = 4 (high 1·medium 1, 전부 신규)
- **확인 C1 (2026-08-10, 적대 예산 소진 후 진입)**: 소멸 확인 = **fp-S1·S3·S4·S5·S6·S7·S8 (7건)**. **fp-S2 잔존**(medium — AC4·§5 "D14 분기 arm"·V1행 "(D7)" 무접두 3곳, 그중 D14 표기는 R1 수정이 재도입) → FIXED 재수정 커밋 `7ebacf9`(지목 3곳 + C-6 D17 참조 명시·ledger 산문 정비) 후 재큐. 신규 finding 없음 · 판정 감사 해당 없음(루프 직접 판정 0건) · verdict **fail**(fp-S2 잔존 사유) → **적대 복귀 1회(상한 밖) + 재진입 확인** 경로.
- **확인 C2 (2026-08-10, 복귀 재진입 — 예산 밖 예약분)**: 소멸 확인 = **fp-S2·S9·S10 (3건 전건)**. 회귀 없음 · 신규 finding 없음 · 판정 감사 해당 없음 · 머지 준비도 verdict **pass**(7 직접 구현 TDD 진행 가능).
- **미확인 FIXED 큐**: **0건** — C1에서 7건(fp-S1·S3~S8) + C2에서 3건(fp-S2·S9·S10) 전건 소멸 확인. **확인 부채 0으로 성공 종료.**
- **루프 직접 판정**: 없음 (fp-S6는 사용자 판정 — C-10 D17)
- **루프 종결(2026-08-10)**: 적대 5 + 복귀 적대 1 / 확인 2(예산 소진 1 + 재진입 예약분 1). disposition 집계 = FIXED 10(전건 소멸 확인) · ESCALATE 1(fp-S6 → 사용자 판정 C-10 D17로 FIXED 귀결) · ACCEPTED/DEFERRED_TO_IMPL/OUT_OF_SCOPE/DUPLICATE/DEFER_LOW 0. 다음 단계로 이월하는 blocking 없음.

## 적대검증 ledger (impl)

- 루프: `review-loop --phase impl` · base = main@`08866bc8d2f30d7b3d7a472cb53fdb113e5d6f1a` · 예산 max 5 / confirm 2 / auto 3 · impl 게이트 = writing-skills TDD GREEN 기록(`.remember/tdd-8-c10.md`) 갈음(planless 트랙 — C-10 D6·D17, ledger 위치 = 이 spec 말미)
- score 산식: spec ledger와 동일(Σ weight, §2c 분류 직후·수정 전 — severity 재평가는 분류의 일부이므로 재평가 후 값으로 계산).

| fingerprint | 발생 | severity | disposition | 근거·수정 |
|---|---|---|---|---|
| fp-I15 · SKILL.md 가드 1·3행 · R4 한정("실재 D블록 = harden 이행 판독")이 수기 완성 D블록의 하한 ② 우회(fail-open) 경로를 만든다는 지적 · rec: 실행 증명 분리 + 확인 복원 + 수기 arm | R1(target `828dbb0`) | high→**medium**(§2c 재평가 — 아래) | FIXED(R1) | 가드 1 "harden 실행 불명" 정의에 **수기 작성 정황**("직접 채웠다"류 발화·기록 — 내용 충실해도 하한 ②상 갈음 불가) 트리거 추가, 수기 정황 없는 실재 D블록의 커밋 이력 재감사 금지(TDD R4 취지)는 유지. **재평가 근거**: ⓐ 무신호 수기 블록은 0.14.0도 동일 통과(이 diff의 회귀 아님) ⓑ 하한 ② 규범 문장이 같은 문면에 실재해 실행이 보완 — RED 4/4 정답으로 실측 ⓒ 닫는 수정 저비용(§2c 문자 갭 규칙 = medium+FIXED). **잔존 경계**: 수기 정황 없는 완성 위조 블록은 grep 수준 판독의 문서 신뢰 모델상 비검출 — fp-I12·I13 ACCEPTED 계열(기록 신뢰 경계, 사용자 기판정)의 소관으로 새 수용 판정을 만들지 않음. 신규 arm X8(R-handd + 수기 발화, 기대 3+확인≤1) RED 4/4 정답→GREEN 파일럿+5런 전건, 재실행 M1·C-P·S7·X3 20/20, **AC2 재측정 0/10**. 수정 커밋 `aa9ebf7` |
| fp-I16 · SKILL.md 3.5행 · 유효 생략 기록이 D번호+근거만 요구 — C-6 D16·ui-mockup 계약의 "사용자가 진입 확인에서 명시적으로 택한 기록만 유효" 요건이 개정에서 소실(이식 표 D16행 요지에 이 절 부재로 AC5 감사도 미검출) · rec: 사용자 결정 표지 한정 + arm | R1(target `828dbb0`) | medium | FIXED(R1) | 3.5행 유효 생략 기록을 "D번호+근거 + **사용자가 진입 확인에서 명시적으로 택했다는 표지**(표지 없는 기록 무효)"로 한정 — C-6 D16 복원. 4·5·6 생략 기록(D2 계약)에는 비확장(X2 재실행으로 확인). RED X9(R-ui-agent — 근거 있음·표지 없음) **4/4 이탈**로 결함 재현(r2는 커밋 이력상 문구 완화 인지에도 인정) → GREEN 재파일럿+5런 전건(파일럿 1런은 픽스처 이력 유출로 폐기·단일 커밋 재구축 — X4 전례), 양성 대조 U2(표지 있는 기록) 5/5 유효 인정·과잉 fail-closed 0, 재실행 X7·X6·X2 15/15. 수정 커밋 `aa9ebf7` |

- **score 이력**: R1 = 2 (medium 2 — fp-I15는 재평가 후 medium으로 계산, 전부 신규·전부 FIXED 후보)
- **미확인 FIXED 큐**: fp-I15 · fp-I16 (2건 — 수정 커밋 `aa9ebf7`, 소멸 확인 대기)
- **루프 직접 판정**: disposition 없음. 단 **fp-I15 severity 재평가(high→medium)와 그 잔존 경계 논거는 루프 판단**이므로 확인 임무 ③ **우선 감사 대상**으로 기록.
- 게이트: TDD GREEN 기록 갱신(임팩트 사이클 유효 60런·폐기 1런 — `.remember/tdd-8-c10.md` §impl R1, 채점 상세 = scores8.md·harness-8 백업). 96줄(D15 상한 내).

**R2 (target `3d95f32`, 2026-08-11)**

| fingerprint | 발생 | severity | disposition | 근거·수정 |
|---|---|---|---|---|
| fp-I17 · SKILL.md 4·6·8행 완료 신호 · 빠른 종료(무-finding) 시 review-loop가 ledger를 남기지 않을 수 있는데(review-loop §1 승계 ledger 부재 분기가 자인) 판별은 종결 ledger만 완료 신호로 읽어 같은 리뷰를 영구 재권고(교착 — 깨끗한 트랙이 9 도달 불가) · rec: 생산자 계약 정합 또는 빠른 종료 표지 신설 + 회귀 arm 3종 | R2 | medium | **ESCALATE(batch-pending)** | fail-closed 방향(재검증 반복 — 검증 생략 아님, AC1 무관). 해법이 갈림: ⓐ review-loop 계약 보강(빠른 종료도 1줄 종결 ledger 기록 — **C-10 범위 밖**, 후속 트랙) ⓑ dev-cycle 판독 정책 확장(루프 실행 정황·주장 vs ledger 부재 괴리를 가드 1 불명 신호로 편입 + arm) ⓒ 병행. batch 시점 일괄 제시 예정 |
| fp-I18 · SKILL.md 7행 · 완료 신호 "task 종결·구현 커밋"이 writing-plans-split 완료 기록 계약(entrypoint status·outcome이 권위, 승인 전 구현 커밋은 완료 근거 아님)과 어긋나 미승인 task를 7 완료로 오인해 8로 통과 가능 · rec: 경로별 완료 신호 분리 + arm | R2 | medium | FIXED(R2) | 7행을 "plan 트랙 = entrypoint task 표의 완료 기록 전건이 권위(승인 전 구현 커밋만으로 완료 아님), planless·표 없는 plan = task 종결·구현 커밋"으로 분리(96줄 유지). RED X10(R-sddopen — task03 구현 커밋 실재·entrypoint 미갱신) **4/4 정답** — 문자 갭이나 실행이 표를 권위로 읽어 보완(fp-I15 동형, §2c 저비용 수정 반영 규칙). GREEN 파일럿+본배치 6/6(r8 비소급 질문 1회 fail-closed 관찰) + 재실행 M4·M5·X4·S6 20/20 무회귀. 픽스처 v1의 baseline 코드 잔재는 스토리 정합 재구축 후 측정(X4 유출 전례). 수정 커밋 `ac9ec8c` |

- **score 이력**: R1 = 2 → R2 = 2 (medium 2 — fp-I17 미판정 + fp-I18 수정 전. 비감소 1회째)
- **미확인 FIXED 큐**: fp-I15·fp-I16(`aa9ebf7`) + fp-I18(`ac9ec8c`) — 3건
- **batch-pending**: fp-I17 1건 (auto-rounds 도달 시 일괄 제시)

**R3 (target `49575ab`, 2026-08-11) — 전환: score 2→2→2(신호 1) + 적대 소진 3 = auto-rounds → batch flush·판정 국면**

| fingerprint | 발생 | severity | disposition | 근거·수정 |
|---|---|---|---|---|
| fp-I17 (재출현) | R2·**R3** | medium | ESCALATE(batch-pending → 일괄 제시) | 동일 계열 2회째 — 리뷰어도 미판정 상태 인지. 반복 규칙상 사용자 판정 확정 대상 |
| fp-I19 · SKILL.md 7행 · R2 수정 문구와 writing-plans-split canonical 계약의 잔존 불일치 — ⓘ canonical 권위 = live SDD progress ledger의 명시 완료 기록(entrypoint 표는 ledger 유실 시 폴백·상충 시 [x]도 미완 회귀)인데 수정 문구는 표를 무조건 권위로 명시 ⓙ SDD 최종 whole-branch 리뷰 종결에 영속 표지가 없어 전 행 [x] 직후 재개 시 조기 8 진입 가능 · rec: 수렴 규칙 정렬 + 종결 신호 요구 + arm | R3 | medium | **ESCALATE(batch)** | ⓘ은 원문 대조로 **사실 확인**(execution contract: "authority = explicit completion record in the SDD progress ledger... falls back to this committed task table"). ⓙ는 생산자측 영속 신호 부재(fp-I17 동류). 7행 표면 2회째(fp-I18 계열) + 판정 국면 → 수정 대신 사용자 판정. 선택지 = 문구 정렬+포인터 FIXED / 현행 ACCEPTED / 생산자 계약 후속 OUT_OF_SCOPE |

- **score 이력**: R1 = 2 → R2 = 2 → R3 = 2 (fp-I17 지속 1 + fp-I19 신규 1 — 2회 연속 비감소, 전환 신호 1 발화. FIXED 후보 0 = 수정 큐 소진, 신호 2 동시 성립)
- **미확인 FIXED 큐**: fp-I15·fp-I16(`aa9ebf7`) + fp-I18(`ac9ec8c`) — 3건 불변 (fp-I19는 fp-I18의 재지적이 아니라 잔존 불일치 신규 지적 — 큐 소멸 판정은 확인 라운드 소관)
- **batch 제시**: fp-I17 + fp-I19 (2026-08-11, 적대 소진 3 도달 시점)

**batch 판정 (2026-08-11, 적대 소진 3 시점 일괄 제시 — 사용자)**

- **fp-I17 → OUT_OF_SCOPE(사용자 판정)**: review-loop 계약 보강(무-finding 빠른 종료에도 1줄 종결 ledger 커밋)을 **후속 트랙으로 이월**. dev-cycle 문면은 현행 유지(거울면 금지·G-b 부합). follow-up은 §4 종료 보고와 `.remember/remember.md` 백로그에 기재 의무.
- **fp-I19 → FIXED(사용자 판정 = 문구 정렬+포인터)**: 7행을 canonical 수렴 규칙에 정렬 — entrypoint 표 완료 기록 + **live SDD 진행 ledger 실재·상충 시 ledger 권위(최종 whole-branch 리뷰 미종결 포함 = 7 미완·SDD 재개)** + ledger 부재·유실 시 커밋된 표 폴백. 신규 arm X11(R-sddfinal — 전 task [x]+.gitignore된 live ledger가 final review PENDING 표시, 기대 7) RED 디스패치됨(r1~r4, 결과 회수 대기). **문면 수정은 미적용 — 재개 세션이 RED 회수 후 적용·검증한다.**

**fp-I19 FIXED 확정 (2026-08-11, 재개 세션 — 수정 커밋 `2cea98c`)**

- **RED 회수**: X11 r1~r4 **4/4 이탈 재현** — 현행 문면에서 전건 "7 완료 → 8 직행"(live ledger의 final whole-branch review PENDING을 인지한 런조차 판정 근거에서 배제). 결함 실증.
- **수정**: 위 합의 문구 그대로 7행 적용(96줄 유지 — D15 상한 내), 하네스 사본 동기.
- **GREEN**: X11 파일럿 r5 + 본배치 r6~r10 **6/6 전건 정답**(전건 7 미완·SDD 재개 — ledger 권위 적용. r10 가드 2 비소급 질문 1회는 신규칙이 종전 판별과 결과가 갈리는 지점의 정당 발동·fail-closed 관찰) · 영향권 재실행 **15/15 무회귀**: X10 5/5(표 폴백 권위 유지, r20 가드 2 질문 1회) · M4 5/5(ledger 부재 폴백 → 8, r18 가드 1 질문 1회 — 픽스처 [x] 선행 커밋 특이점의 fail-closed 확인) · X4 5/5(9 유지 — 신설 폴백 문구 비간섭). 오염 검사 4종 porcelain clean · AC1 이탈 0 유지 · 유효 25런(RED 4+파일럿 1+본배치 20)·폐기 0.
- **미확인 FIXED 큐 편입**: fp-I19(`2cea98c`) — 큐 4건(fp-I15·fp-I16(`aa9ebf7`) + fp-I18(`ac9ec8c`) + fp-I19(`2cea98c`)). 수정 큐 소진 — 확인 모드 진입.

**확인 C1 (2026-08-11, target `33eba48` — 완전한 응답)**

- **소멸 확인 = fp-I15·fp-I16·fp-I18·fp-I19 (4건 전건)** — fp-I15: 가드 1이 수기 D블록을 harden 갈음으로 인정하지 않음·재감사 금지 유지 / fp-I16: 3.5행 표지 요건·표지 없는 기록 무효 명시 / fp-I18: 승인 전 구현 커밋 불인정이 fp-I19 정렬 후에도 보존 / fp-I19: live ledger 권위·표 폴백이 canonical과 정렬.
- 회귀 없음(가드 1 기존 트리거·3.5 비대상 경로·4·5·6 생략 계약 비확장·planless 직접 구현 경로·첫 미완 규칙 전건 유지) · 신규 finding 없음 · **판정 감사 이상 없음**(fp-I15 재평가·fp-I18 medium 유지 모두 §2c 비례 판정 승인) · 머지 준비도 verdict **pass**.
- **미확인 FIXED 큐: 0건 — 확인 부채 0으로 성공 종료.**

**루프 종결 (2026-08-11)**: 적대 3(R1~R3) / 확인 1(C1). disposition 집계 = **FIXED 4**(fp-I15·I16·I18·I19 — 전건 소멸 확인) · **OUT_OF_SCOPE 1**(fp-I17 — 사용자 판정, follow-up = review-loop 계약 보강 트랙: 무-finding 빠른 종료에도 1줄 종결 ledger 커밋) · **ESCALATE 2**(fp-I17·fp-I19 — 전건 사용자 batch 판정으로 귀결) · ACCEPTED/DEFERRED_TO_IMPL/DUPLICATE/DEFER_LOW 0. score 이력 R1 = 2 → R2 = 2 → R3 = 2(전환 신호 1 → batch → 확인). 다음 단계로 이월하는 blocking 없음.
