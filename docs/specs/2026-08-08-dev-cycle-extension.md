# dev-cycle 확장 설계 (C-6 ①②③)

- 상태: **harden-spec 종결 (2026-08-08)** — 갭 대장 소진(D1~D19 확정). 다음 = `review-loop --phase spec`
- 대상: `dev-workflow` 플러그인 **기존** 스킬 `dev-cycle` 개정 (기준 0.10.0). `harden-spec`은 연동 검토 결과 **무수정**(D6)
- 날짜: 2026-08-08
- 입력: `docs/specs/2026-08-07-review-loop-and-pipeline-improvements-brief.md` §실행 순서 5·6번 / §트랙 C C-6
- 승인 이력: **"순번 1~7을 순차 진행한다"가 기결정**(브리프 §실행 순서, 2026-08-07 사용자 결정). **순번 5(C-6③)와 순번 6(C-6①②)의 한 릴리스 흡수 = 2026-08-08 사용자 결정** — 셋 다 같은 `dev-cycle/SKILL.md` 단일 파일을 고치므로, 브리프가 순번 1+3에 허용한 흡수 전례와 동형이다(전 항목 순차 진행 결정과 모순 없음). C-6의 세 항목 내용은 브리프의 *제안*이었고 harden-spec 2026-08-08에서 D1~D19로 확정했다.
- 경로 결정: 트랙 A·B·C-5 전례대로 plan phase 생략 — **writing-skills TDD 직접 구현**. 순서 = 브리프 변환 → harden-spec → `review-loop --phase spec` → 구현(TDD) → `review-loop --phase impl` → 릴리스(0.11.0 + README 3종). **이 경로 선택 자체가 D2·D19의 자기적용**이다(§3 D19).

## 1. 배경 / 문제

dev-cycle은 0.1.0 이후 **읽기 전용 지도**로만 존재했다. ops-hub 전수 점검(브리프 §1·§트랙 C C-6 행)과 2026-08-08 재조사에서, 이 지도가 **실채택되지 않고**(①②) **지도 밖 구간에서 규약 이탈이 반복**(③)되는 것이 실측됐다.

| # | 신호 | 실측 (2026-08-08 재조사) | 문제 |
|---|---|---|---|
| ③ | **파이프라인 통째 우회** | 2026-08-01~03 창에서 spec 8건 중 **harden 3/8 · ledger 2/8**. 우회군: `report-management-hub`(7 task) · `menu-tree-ledger`(1) · `user-management-ui-redesign`(3) · `workflow-claims-list-consolidation`(5) · `master-password-dev-login`(**plan 자체 없음**). **우회군 5건 전부 impl 적대검증 ledger 0** — spec harden·spec 루프·impl 루프를 통째로 건너뛰었다 | 지도에 **8단계 전체 아니면 없음**의 이분법만 있어, 무겁다고 느끼면 통째로 우회한다. 우회의 대가가 실증됨 — `report-management-hub`가 유일하게 받은 검증은 **PR #175 외부 사람 리뷰**였고, 그것이 **사후 수정 plan `2026-08-02-report-management-review-fixes`(4 task)** 를 낳았다 |
| ① | **8단계 이후가 지도에 없음** | `docs/` 전체에서 "육안" **120회**. `docs/design-qa.md`는 배포 후 사용자 브라우저 육안 대조 기록의 축적처. ops-hub `CLAUDE.md` #4·#7·§dev 테스트 배포는 **PR·머지·ff pull·배포 절차**를 규약화해 두었다 | 지도가 8단계(impl 적대검증)에서 끝나는데 실제 트랙 종료 조건은 **PR·머지·배포·실측**이다. 지도 밖이라 매 트랙이 "잔여=육안"을 즉흥 처리하고, 그 부채가 문서에 누적된다 |
| ② | **dev-cycle 실채택 얕음** | ops-hub `docs/` 91 spec 중 dev-cycle 언급 **4파일** — 그중 3건이 **"dev-cycle 3.5 생략 기록"** 인용, 1건이 "dev-cycle로 분할 작성" 예고. 단계 번호만 인용될 뿐 지도로서 소비되지 않는다 | 지도는 있으나 **트랙 착수 시 넘겨줄 산출물이 없다**. ops-hub는 이 공백을 스킬 밖에서 발명했다 — `CLAUDE.md` #9: "설계·실측을 main에서 했더라도 결과를 **브리프**로 써서 넘긴다 … 설계 결론·실측 근거·변경 대상 파일·검증 방법·완료 조건(PR 머지·배포까지)". C-1(런북 흡수)과 같은 패턴 |

**핵심 관찰 (브리프 서술의 정정):** 브리프는 ③을 "**소형** 트랙 파이프라인 통째 우회"로 적었으나, 실측상 우회는 **규모와 무관**했다 — 우회군에는 1 task 트랙과 **7 task 트랙(사후 수정 plan 유발)이 함께** 있다. 따라서 경량 경로의 목적은 "소형이면 건너뛰어도 된다는 승인"이 아니라 **우회의 하한선(어떤 트랙이든 최소 무엇은 남긴다) + 분기 기준의 명시화**다. 이 정정이 D1(분기 축 = 접촉 표면·가역성, 규모 아님)의 근거다.

## 2. 목표 / 비목표

**목표:** dev-cycle 지도를 **전 구간(착수 → 종료)** 으로 확장하고, **전부 아니면 무(無)** 이분법을 없앤다.

- ① **9단계 신설** — 통합·후속 검증(PR·머지·배포·실측)을 지도에 넣고, 각 repo 규약을 느슨 참조한다(D8~D10).
- ② **착수 브리프 생성 안내** — 트랙 착수 시 다음 세션에 넘길 브리프의 필수 항목을 지도가 안내한다(읽기 전용 출력, D12~D14).
- ③ **경량 경로(fast lane) 공식화** — 분기 축(D1)·입도(D2)·하한(D3)·승격(D4)·판정 주체(D5)를 규약화한다.

**비목표 (YAGNI / 기결정 준수):**

- **dev-cycle을 오케스트레이터로 만들기** — 현행 스킬 문면의 기결정("이건 읽기 전용 지도다", "한 세션에서 전 과정을 자동으로 돌리지 않는다")이자 ops-hub `docs/specs/2026-06-20-adversarial-review-loop-workflow-design.md:45`의 보류 결정("메가스킬은 깨지기 쉽다"). 확장해도 **파일을 고치지 않는 지도**라는 성격은 불변 — 착수 브리프도 출력만 한다(D12).
- **기계 검증 장치(파서·스위트·자동 게이트·훅) 신설** — 브리프 §기결정 대조(G1·G2) 준수. 9단계 완료 판정도 grep·`gh` 조회 수준을 넘지 않고, 불명확하면 사람에게 묻는다(D9).
- **PR·머지·배포 절차 자체의 규약화** — 9단계는 **repo 규약을 가리키는 포인터**다(D8). 절차 내용(머지 컨벤션·배포 명령·체크 항목 열거)은 각 repo AGENTS.md/CLAUDE.md 소유이며 이 스킬이 복제하지 않는다 — 중복 서술 = 드리프트(ops-hub CLAUDE.md:9의 자기 교훈).
- **dev-cycle 외 스킬 문면 개정** — `review-loop`(C-1~C-3)·`ui-mockup`(C-7)·훅(C-8)·`setup`(C-9)은 순번 7 범위. 이번 압박에서 드러난 두 접점도 **미수정으로 확정**했다: setup 마커 문구의 "큰 변경"(D11) · review-loop impl 게이트의 npm 고정(D7 — dev-cycle 안내로 처리).
- **superpowers 스킬 수정** — 외부 플러그인. 9단계는 `finishing-a-development-branch`를 **느슨 참조**만 한다(D10).
- **각 repo CLAUDE.md·AGENTS.md 수정** — 각 repo 소유. C-5 D8 전례대로 종료 보고 안내 1줄까지.
- **기존 트랙 소급 적용** — 지도는 착수 시점 안내다. 진행 중·완료 트랙을 경량/정식으로 재분류하지 않는다.
- **단계 경계 = 새 세션 + `/clear`** 규약 완화 — 정착 자산. 경량 경로는 *단계를 생략*할 수 있을 뿐 남은 단계의 경계 규약을 바꾸지 않는다(D2).
- **경량 경로의 산출물 서식 신설** — spec-lite는 **새 서식이 아니라** 기존 review-loop spec 게이트 6항목 + 재논의 금지 블록이다(D3). 별도 템플릿·전용 디렉터리를 만들지 않는다.

## 3. 설계 확정 (harden-spec 2026-08-08 · D1~D19)

### 경량 경로 (C-6③)

**D1. 분기 축 = 접촉 표면·가역성** — 스키마·마이그레이션·권한·외부 연동·비가역 작업에 닿는가로 판정한다. **task 수(규모)는 기준이 아니다.**
- 근거: §1 핵심 관찰 — 사후 수정 plan을 부른 것은 7 task 트랙이고 1 task 트랙은 무사고였다. 규모 기준은 실측에 반증된다. 이 어휘는 review-loop §1의 "보안 크리티컬 자가 판정"(권한·인증·보안 경계·데이터 손상/유실·비가역 마이그레이션)과 같은 신호를 쓰므로 두 스킬이 같은 축을 공유한다.

**D2. 입도 = 하한 고정 + 단계별 선택** — 경량은 정해진 생략 패키지가 아니다. **하한(D3)은 고정**하고 그 위 단계(spec 적대검증·plan·plan 루프)는 각각 생략할 수 있되, **생략할 때마다 spec의 `## 재논의 금지(기결정)`에 D번호로 근거를 남긴다.**
- 근거: 실증된 경량 운용은 두 형태다 — 이 repo의 "plan만 생략"(트랙 A·B·C-5)과 ops-hub 08-01~03의 "전부 생략". 고정 패키지로 묶으면 전자가 규격 밖(정식인데 plan이 없음)이 된다. 생략을 D번호로 남기는 것은 ui-mockup이 이미 쓰는 형태(ops-hub 3건 실측)라 새 장치가 아니다.

**D3. 하한 = spec(게이트 6항목 + D블록) + impl review-loop** — 어떤 경량 트랙도 이 둘은 생략하지 못한다.
- **spec 문서**: review-loop spec 게이트 6항목(목표·범위·비목표·결정사항·acceptance criteria·미해결 질문) + `## 재논의 금지(기결정)` 블록. **새 서식을 발명하지 않고 이미 있는 게이트를 재사용**하므로, 경량→정식 승격 시 그 spec을 그대로 spec 루프에 넣을 수 있다.
- **impl review-loop**: 우회군 5건 전부 impl ledger 0이었고, 그중 유일하게 외부 리뷰를 받은 트랙이 사후 수정 plan을 낳았다.

**D4. 승격 = 접촉 표면이 드러나면 즉시** — 경량 진행 중 스키마·권한·마이그레이션·외부 연동·비가역 접촉이 새로 드러나면 그 자리에서 정식 경로로 올리고 spec에 사유를 기록한다. D1과 같은 신호를 쓰므로 별도 임계치를 두지 않는다(task 증식·라운드 초과 같은 진행 신호는 트리거가 아니다 — 규모 기준을 뒷문으로 되돌리지 않기 위함).

**D5. 판정 주체 = 지도 1차 판정 + 사용자 확인 1회** — dev-cycle이 접촉 표면 신호로 1차 판정하고 사용자에게 확인 1회를 받는다. review-loop §1의 "보안 크리티컬 자가 판정 — 확인 1회"와 같은 모양이다.
- 근거: 경량 오판정은 **검증을 건너뛰는 방향**의 오류라 비대칭적으로 위험하다. 확인 1회가 그 방향만 막는다.

**D6. harden-spec 무수정** — 경량 트랙에서도 harden-spec을 그대로 돌린다. lite 모드를 신설하지 않는다.
- 근거: harden-spec의 핵심 원칙은 "질문은 예산이 아니라 커버리지"다. 접촉 표면이 없는 트랙은 갭 대장 자체가 작아 **자연히 짧아진다** — 모드 없이도 경량이 성립한다. dev-cycle은 "경량이어도 harden-spec은 하한(D블록의 출처)"이라고 안내만 한다.

**D7. 비-npm repo의 impl 게이트 = dev-cycle이 갈음 안내** — review-loop impl 게이트는 npm 4종 고정이므로, 경량 경로 절에 "비-npm repo는 그 repo의 검증 명령 또는 writing-skills TDD GREEN 기록으로 갈음한다" 한 줄을 둔다. **review-loop 문면은 고치지 않는다**(순번 7 범위 보존).
- 근거: 이 repo 자신이 트랙 B·C-5에서 "writing-skills TDD GREEN 기록으로 갈음"을 이미 썼다(50 runs variance 0).

**D16. ui-mockup(3.5)은 경량에서도 생략 불가** — 화면을 만들거나 기존 화면 구성을 바꾸면 경량이어도 3.5를 돌린다(현행 §판별 3번 유지). 단 **화면 변경이 정식 승격을 부르지는 않는다** — 3.5만 추가된다.
- 근거: UI 우회군 3건(`user-management-ui-redesign`·`workflow-claims-list-consolidation`·`ui-design-system`) 전부 `## UI 설계` 절 0이고 `docs/design/`에도 없다. 그중 `ui-design-system`은 전사 디자인 체계 변경이었다. 반대로 화면 변경 전체를 접촉 표면(D1)에 넣으면 문구·색 수준 변경까지 정식으로 끌어올려 경량이 비게 된다(현행 3.5도 경미 변경은 건너뛰라고 규정).

**D17. impl ledger 위치 = plan이 없으면 spec 문서 말미** — 경량으로 plan을 생략한 트랙의 `## 적대검증 ledger (impl)`은 spec 문서에 신설한다.
- 근거: 이 repo가 트랙 B·C-5에서 이미 그렇게 했고 두 번 다 성공 종결했다. **순번 7(C-3)이 "impl ledger = plan 엔트리포인트 말미"를 규약화할 때 "plan이 없는 경량 트랙은 spec 문서"를 예외로 승계해야 한다** — 이 결정을 그 트랙에 넘긴다.

**D18. 9단계는 경로 무관 공통** — 경량 트랙도 PR·머지·배포·실측을 거친다. 경량은 *설계·검증 단계*를 줄이는 것이지 통합·배포를 면제하지 않는다.
- 근거: 우회군이 사후 수정을 부른 지점이 바로 PR 리뷰 이후 구간이었다.

**D19. 자기적용 = 이 트랙은 plan만 생략하고 spec 루프는 정식으로 돈다** — 이 트랙(C-6)은 D1 기준 접촉 표면 0이라 경량 판정 대상이며, D2의 단계별 선택에 따라 **plan·plan 루프만 생략**한다. spec 적대검증은 생략하지 않는다.
- 근거: 아직 검증되지 않은 규격을 그 규격 자신의 검증에 쓰는 순환을 피한다. 동시에 이것이 **경량 규격의 첫 자기적용 사례**로 기록된다(§8 관찰).

### 9단계 — 통합·후속 검증 (C-6①)

**D8. 범위 = PR·머지·배포·후속 실측 전부, 단 포인터로만** — 9단계는 트랙 종료까지를 지도에 넣되, 절차 내용은 각 repo 규약(CLAUDE.md/AGENTS.md)을 가리킨다. **체크 항목을 열거하지 않는다.** 배포가 없는 repo(이 플러그인 등)에서는 릴리스 + `/plugin update` 안내가 그 자리를 대신한다.
- 근거: ops-hub CLAUDE.md #4("트랙 종료 = push + PR + 머지 + 로컬 브랜치 삭제까지")·#7(ff pull·배포). 절차 복제 금지는 ops-hub 자신의 교훈(CLAUDE.md:9 — 중복 서술이 드리프트를 낳아 2026-08-02에 AGENTS.md로 통합).

**D9. 완료 판정 = PR merged + repo 규약이 요구하는 후속, 불명확하면 사용자 확인** — 배포·실측 여부는 repo 밖 사실이라 조회로 확정되지 않는다. 현행 §판별이 이미 "애매하면 사용자에게 확인"으로 끝나므로 같은 폴백을 쓴다.

**D10. 지목 스킬 = `superpowers:finishing-a-development-branch` + repo 규약 포인터 병기** — 9단계에 대응 스킬이 없다는 초안 전제는 사실이 아니었다(스킬 실재). dev-cycle이 1·7단계에서 이미 superpowers를 느슨 참조하므로 구조가 동형이고, "superpowers 미설치 시 대체 가능" 폴백 문면도 그대로 적용된다.

**D11. setup 미수정** — setup 마커 템플릿의 "새 기능·**큰 변경**을 시작하면"이 경량 트랙을 배제하는 것처럼 읽히지만, 이는 트리거 예시일 뿐이고 진입 후엔 dev-cycle 자신이 경량을 안내하므로 실해가 작다. 비목표(dev-cycle 외 스킬 개정 = 순번 7)를 지킨다. 종료 보고 안내 1줄로 대신한다.

### 착수 브리프 (C-6②)

**D12. 출력 형태 = 화면 출력만** — 항목 틀을 화면에 제시하고 파일은 만들지 않는다. "지도를 이유로 파일 수정 금지"라는 dev-cycle 기결정을 그대로 지킨다. 저장 여부·위치는 사용자 몫이다.

**D13. 필수 항목 = ops-hub 실증 5종** — 설계 결론 · 실측 근거 · 변경 대상 파일 · 검증 방법 · 완료 조건(PR 머지·배포까지). 마지막 항목이 D8(9단계 범위)과 자연히 맞물린다.
- 근거: ops-hub CLAUDE.md #9가 스킬 밖에서 발명해 쓰고 있는 목록 그대로 — C-1(런북 흡수)과 같은 패턴이다.

**D14. 발동 시점 = 트랙 착수 시 1회 + 핸드오프와 역할 경계 명시** — 단계 경계의 인계는 review-loop 핸드오프 소관임을 문면에 밝혀 중복을 끊는다. 둘은 다른 문서다 — **브리프는 착수용(설계 결론), 핸드오프는 재개용(루프 상태)**.

### 공통

**D15. 분량 상한 100줄** — 현재 56줄. 표 확장을 우선하고 신설 절은 2개(경량 경로·착수 브리프)까지. 상한 초과 시 내용을 줄이는 게 아니라 절을 통합해 줄인다. "한 화면에 들어오는 지도"라는 성격이 이 스킬의 자산이다.

## 4. 현행 스킬 접점 — 개정 지도

| 위치 | 현행 | 확정 개정 |
|---|---|---|
| `dev-cycle/SKILL.md` §권장 파이프라인 | 표 8행 + 주석 3개 | **9단계 행 추가**(D8) + 9단계 스킬 열에 `finishing-a-development-branch`(D10) |
| 〃 §단계 경계 = 새 세션 + `/clear` | spec→plan·plan→impl 경계 | 경량이 경계 규약을 바꾸지 않음을 1줄 명시(D2 비목표) |
| 〃 §지금 어느 단계인가 (판별) | 1~6번 판별 규칙 | **9단계 판별 추가**(D9 — PR merged + repo 후속, 불명확시 확인) + **경량/정식 분기 판정**(D1·D5) |
| 〃 §하지 말 것 | 3항 | 경량 오용(표면 접촉 트랙을 경량으로)·브리프 파일 생성(D12)·절차 복제(D8) 관련 항 추가 |
| 〃 (신설 1) | — | **경량 경로 절** — 분기 축(D1)·입도와 기록(D2)·하한(D3)·승격(D4)·3.5 예외(D16)·비-npm 갈음(D7)·ledger 위치(D17)·9단계 공통(D18) |
| 〃 (신설 2) | — | **착수 브리프 절** — 5항목(D13)·출력만(D12)·발동 시점과 핸드오프 경계(D14) |
| `harden-spec/SKILL.md` | 느슨한 계획 모드 보유 | **무수정**(D6) |
| `setup/SKILL.md` | 마커 문구 "큰 변경" | **무수정**(D11) |
| `review-loop/SKILL.md` | impl 게이트 npm 고정 | **무수정**(D7 — dev-cycle이 갈음 안내) |
| `description` (dev-cycle) | 트리거 예 5종 | 경량 트리거 1~2개 추가 검토(writing-skills 원칙상 **트리거만**, 워크플로 요약 금지) |
| README ×3 | 표 9행 · 목차 22행 · §1 67~75행 | 3언어 동기 갱신(9단계·경량 경로·브리프). **기존 번호 1~8·3.5는 불변이라 ui-mockup·harden-spec·README의 "3.5" 인용은 깨지지 않는다**(조회 확인) |

## 5. 실측 현황 (2026-08-08 조회)

| 항목 | 값 | 조회 |
|---|---|---|
| ops-hub spec 총수 | 91 | `ls docs/specs/*.md` |
| 그중 `재논의 금지` 블록 보유 | 42 (46%) | `grep -rl` |
| 그중 `적대검증 ledger` 보유 | 47 (52%) | `grep -rl` |
| 2026-08-01~03 창 | spec 8건 중 harden 3 · ledger 2 · **impl ledger 0/5(우회군 전건)** | 파일별 조회 |
| 2026-08-04~08-06 창 | spec 7건 **전량** harden + ledger | 〃 (정식 경로 복귀 구간) |
| 2026-08-08 `ui-design-system` | harden 0 · ledger 0 (5 task plan 보유) | 〃 (우회가 08-03에서 끝나지 않았음) |
| UI 우회군 3건 | `## UI 설계` 절 **0/3** · `docs/design/`에도 없음 | 〃 (3.5도 우회 — D16 근거) |
| plan 엔트리포인트 총수 | 89 | `ls docs/plans/*.md` |
| "육안" 언급 | 120회 | `grep -rn 육안 docs/` |
| dev-cycle 언급 | 4파일 (3건이 "3.5 생략 기록") | `grep -rn dev-cycle docs/` |

## 6. 검증 계획 (D9 전례 승계 — writing-skills TDD)

브리프 §0-2가 요구하는 TDD. 트랙 A·B·C-5와 동형이며 실호출·실배포 대조는 불요(문면이 안내만 하므로).

1. **RED / 베이스라인** — 현행 0.10.0 dev-cycle로 재현한다: (a) 접촉 표면 없는 소형 변경 착수 시 지도가 8단계 전체를 요구해 우회를 유발하는지 (b) impl 루프 종결 후 "다음은?"에 9단계 안내가 없는지 (c) 착수 브리프 안내 부재.
2. **GREEN 마이크로테스트** — 개정판으로 **시나리오별 5+ reps · no-guidance 대조군 포함 · variance 0**. 시나리오:
   - 경량 판정 — 접촉 표면 없음(경량) / 스키마·권한 접촉(정식) 각 경계 케이스, 그리고 **7 task이지만 표면 접촉(정식)** · **1 task이지만 마이그레이션(정식)** 으로 규모 축 오염 여부 확인(D1).
   - 하한 준수 — 경량 판정 후에도 spec 6항목+D블록·impl 루프를 요구하는가(D3).
   - 승격 — 진행 중 스키마 접촉이 드러난 시나리오에서 즉시 승격 안내(D4).
   - 3.5 예외 — 경량 + 화면 구성 변경에서 3.5를 요구하되 정식 승격은 부르지 않는가(D16).
   - 9단계 — impl 루프 종결 상태에서 9단계 안내 + `finishing-a-development-branch` 지목(D10), 배포 없는 repo에서 릴리스로 대체(D8).
   - 브리프 — 5항목 열거 + **파일 생성 시도 0**(D12).
   - 비-npm 갈음 안내(D7).
3. **위반 픽스처** — 경량 오용(마이그레이션 트랙을 경량으로 착수) · 규모만 큰 무해 트랙(정식 오판정) · 브리프 파일 생성 유도 프롬프트.
4. **분량 검사** — 개정 후 `wc -l ≤ 100`(D15).

## 7. 완료 조건 (acceptance criteria)

- dev-cycle SKILL.md에 D1~D19가 반영되고 **100줄 이하**(D15). harden-spec·setup·review-loop는 **무수정**으로 유지된다(D6·D11·D7).
- writing-skills 마이크로테스트 GREEN(시나리오별 5+ reps·variance 0) + 베이스라인 대비 개선 실증.
- `review-loop --phase spec` 종결(미판정 blocking 0 · 미확인 FIXED 큐 0) → 구현 → `review-loop --phase impl` 종결.
- 릴리스 **0.11.0** + README 3종(en/ko/ja) 동기 갱신 + 종료 보고에 각 머신 `/plugin update` 안내.
- 종료 보고에 **순번 7(C-3·C-4·C-7·C-8·C-9)로의 승계 사항 2건** 명시: D17(경량 트랙 impl ledger = spec 문서 말미)을 C-3이 예외로 흡수할 것 · D11(setup 마커 문구)을 C-9에서 처리할 것.

## 8. 이 트랙에서 관찰할 것

- **경량 규격의 첫 자기적용**(D19) — 이 트랙이 plan만 생략하는 형태로 규격 안에 들어오는지, spec 루프 적대검증자가 이를 규격 위반으로 지목하는지.
- **기결정 가드 focus 주입(0.9.0) 3차 데이터** — 트랙 B(spec 재지목 4) → C-5(spec 1 / impl 6건 중 4건이 자기유발) 흐름에 이 트랙 수치를 더한다.
- **흡수 릴리스의 비용** — 5+6 통합이 spec·TDD 범위를 얼마나 키웠는지(시나리오 수·라운드 수)를 기록해, 순번 7(5개 항목 묶음)의 흡수 판단 근거로 남긴다.
- **0.10.0 plan 형식 관문의 미적용** — 이 트랙은 plan을 생략하므로 관문 4종·완료 기록 계약 ④가 발동하지 않는다. 그 실적용 관찰은 다음 SDD 경유 트랙으로 이월한다.

## 재논의 금지(기결정)

**승계된 기결정 (이 트랙에서 재론 금지)**

| # | 기결정 | 출처 |
|---|---|---|
| G-a | 브리프 §실행 순서 1~7 순차 진행 · 순번 5+6 한 릴리스 흡수 | 2026-08-07·08-08 사용자 결정 |
| G-b | 기계 검증 장치(파서·스위트) 과설계 금지 — grep 수준 경량 확인 | 브리프 §기결정 대조 G1·G2 |
| G-c | description은 **트리거만** — 워크플로 요약 금지 | writing-skills 원칙 |
| G-d | no-AI-trace — 커밋·산출물에 AI 서명·도구 흔적 금지 | 글로벌 규칙 |
| G-e | 스킬 문면 변경은 **writing-skills TDD 필수**(베이스라인 → 개정 → 5+ reps 마이크로테스트) | 브리프 §0-2 |
| G-f | 릴리스 = minor 범프 + README 3종 동기 갱신 + `/plugin update` 안내 | 브리프 §0-3 |
| G-g | dev-cycle은 **읽기 전용 지도**이며 오케스트레이터가 아니다 | 현행 SKILL.md + ops-hub 2026-06-20 spec:45 |
| G-h | 단계 경계 = 새 세션 + `/clear` | 현행 SKILL.md · review-loop 규약 |
| G-i | plan 형식 관문은 **spec 절차 준수를 강제하지 않는다**(전 phase 루프 실행 여부 미검사) · 승계 ledger 부재 시 ③ 스킵 + 1줄 기록 | C-5 D11 — **경량 경로와의 충돌을 이미 방지해 둔 조항** |
| G-j | 분할 plan 구조·ESCALATE batch·disposition 6종은 정착 자산 — 건드리지 않는다 | 브리프 §1·§기결정 대조 |

**이번 harden-spec에서 확정 (D1~D19)** — 본문 §3이 원본이다. 요약:

| # | 결정 |
|---|---|
| D1 | 경량 분기 축 = 접촉 표면·가역성 (규모 아님) |
| D2 | 입도 = 하한 고정 + 단계별 선택, 생략마다 D번호 기록 |
| D3 | 하한 = spec(게이트 6항목 + D블록) + impl review-loop |
| D4 | 승격 = 접촉 표면이 드러나면 즉시 (진행 신호는 트리거 아님) |
| D5 | 판정 = 지도 1차 + 사용자 확인 1회 |
| D6 | harden-spec 무수정 (경량에서도 하한) |
| D7 | 비-npm impl 게이트 = dev-cycle이 갈음 안내, review-loop 무수정 |
| D8 | 9단계 범위 = PR·머지·배포·실측 전부, 포인터만(절차 복제 금지) |
| D9 | 9단계 완료 판정 = PR merged + repo 규약 후속, 불명확시 사용자 확인 |
| D10 | 9단계 지목 = `superpowers:finishing-a-development-branch` + repo 규약 포인터 |
| D11 | setup 미수정 (마커 문구는 순번 7 C-9로) |
| D12 | 착수 브리프 = 화면 출력만, 파일 생성 없음 |
| D13 | 브리프 필수 5항목 = 설계 결론·실측 근거·변경 대상 파일·검증 방법·완료 조건 |
| D14 | 브리프 발동 = 트랙 착수 시 1회, 핸드오프(재개용)와 역할 경계 명시 |
| D15 | dev-cycle SKILL.md 분량 상한 100줄 |
| D16 | ui-mockup 3.5는 경량에서도 생략 불가 (단 정식 승격은 아님) |
| D17 | 경량(plan 없음) impl ledger = spec 문서 말미 — 순번 7 C-3이 예외로 승계 |
| D18 | 9단계는 경로 무관 공통 |
| D19 | 자기적용 = 이 트랙은 plan만 생략, spec 루프는 정식 |

## 잔여 리스크 (DEFERRED)

**DEFERRED(사용자 유예): 0건** — 모든 판단 갭이 질문으로 닫혔다.

**검증 필요(실측) 2건** — 조회로 답이 결정되지 않고 사용자가 결정할 수도 없는 외부 사실. 구현·전환 단계 이후의 실측 항목으로 남긴다.

| # | 항목 | 언제 측정 |
|---|---|---|
| V1 | **경량 경로가 실제로 우회를 줄이는가** — 규격을 만든다고 우회가 사라진다는 보장은 없다. 08-01~03 우회는 규약 부재가 아니라 *비용 회피*였을 수 있고, 그렇다면 경량 경로가 "정식의 대안"이 아니라 "우회의 새 이름"이 된다 | 0.11.0 이후 ops-hub 트랙에서 harden·ledger 보유율과 경량 판정 건수를 재조사 |
| V2 | **9단계 안내가 "잔여=육안" 부채 누적을 줄이는가** — 지도에 넣는다고 실측이 수행된다는 보장은 없다(dev-cycle 실채택 자체가 얕았다는 것이 §1 ②) | 같은 시점에 `docs/`의 "육안" 언급 증가율과 design-qa 기록 시점 대조 |
