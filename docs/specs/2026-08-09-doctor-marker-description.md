# 순번 7b 설계 — doctor 점검 신설 · setup 마커 문구 · dev-cycle description (C-9 · C-6 D11 · fp-I3)

- 상태: **4단계 `review-loop --phase spec` 진행 중 (2026-08-09)** — 3단계 harden-spec 종결(갭 21건 소진, DEFERRED 0, 검증 필요 1), §3의 결정에 **D1~D21 부여 완료**(루프에서 사용자가 닫은 ESCALATE 2건 = **D22·D23** 추가). 루프 ledger = 이 문서 말미 `## 적대검증 ledger (spec)`.
- 이력: 7a 트랙 종결(0.12.0, `3267b82`)에서 이 트랙을 다음 순번으로 지목
- 대상: `dev-workflow` 플러그인 (기준 0.12.0) — **신설** `skills/doctor/SKILL.md` · **개정** `skills/setup/SKILL.md`(마커 문구) · `skills/dev-cycle/SKILL.md`(description) · `.claude-plugin/plugin.json` · README 3종
- 날짜: 2026-08-09
- 입력: `docs/specs/2026-08-07-review-loop-and-pipeline-improvements-brief.md` §실행 순서 7b / §트랙 C의 C-9 + C-6이 승계한 D11 · fp-I3
- 승인 이력:
  - **"순번 1~8을 순차 진행한다"가 기결정**(브리프 §실행 순서 — 1~7은 2026-08-07, 순번 8은 2026-08-09 사용자 결정).
  - **순번 7의 7a/7b 분할 = 2026-08-09 사용자 결정.** 7b는 발견성 TDD를 새로 요구해 하네스 계열이 다르다.
  - **doctor 발동 경로 = 명시 호출 + 증상 트리거** (2026-08-09 사용자 결정).
  - **doctor 조치 경계 = 진단 전용 + 위임** (2026-08-09 사용자 결정).
  - **버전 비교 기준 = 이 머신 vs 마켓플레이스 최신** (2026-08-09 사용자 결정).
  - **구현 형태 = 스킬 문면에 조회 명령 명시(A안)** (2026-08-09 사용자 결정).
- 경로 결정: **경량 경로 — 4단계 수행, 5·6단계 생략**(2026-08-09 사용자 승인, **세션 한정**). 접촉 표면(스키마·마이그레이션·권한·인증 경계·외부 연동·데이터 손상/유실·비가역) 어디에도 닿지 않는다 — doctor는 **사용자 작업물·설정을 쓰지 않는다**(D9; 마켓플레이스 clone `fetch`만 예외이며 가역·무해다). 5를 생략하므로 6은 자동으로 해당 없음이다. 순서 = 브리프 변환 → `harden-spec` → `review-loop --phase spec` → writing-skills TDD 직접 구현 → `review-loop --phase impl` → 릴리스 0.13.0.

## 1. 배경 / 문제

C-9의 근거는 브리프 `:149` 한 줄이다 — *"3머신 버전 불일치 상시 위험(그램 최초 install 백로그 잔존, 0.7.0 미만 조기 넛지 버그)"*.

**실측된 실패 3종** (전부 "조용히 틀린 상태" — 에러 없이 결과만 잘못된다):

| 실패 | 실측 |
|---|---|
| 플러그인 버전 불일치 | 각 머신이 직접 `/plugin update`를 해야 하는데 상시 밀린다. **2026-08-09 실측**: 맥북 user-scope만 0.12.0이고 OMEN·그램은 0.9.0~0.12.0 미적용, **ops-hub project-scope는 0.11.0에 고정**. 매 트랙 완료 조건(브리프 `:165`)이 `/plugin update` 안내를 요구하는데도 밀리므로 **안내만으로는 닫히지 않는다** |
| `CLAUDE_CTX_LIMIT` 미설정 | 훅은 윈도 크기를 런타임에 알 수 없다(모델 ID가 베어 ID라 `[1m]` 접미사가 없고 usage에도 윈도 값이 없음 — `setup/SKILL.md:14`). 기본 1M 가정이라 **200k 세션은 사용량이 5배 과소평가돼 넛지가 아예 안 뜬다.** **2026-08-09 실측**: 이 머신은 settings.json 3곳 어디에도 값이 없다 |
| codex 인증·실행 가능성 | 파이프라인 4·6·8단계가 전부 codex 적대검증이다. 인증이 끊기면 검증 축이 통째로 멈춘다. 7a impl 루프의 **codex "정지" 오진 3회**가 같은 계열 — 진단 경로가 없어 원인을 codex로 잘못 짚었고 공유 broker를 1회 잘못 종료했다 |

**README에 이미 트러블슈팅 4항목이 있다**(`README.ko.md:205~210`). 즉 지식이 없는 게 아니라 **대조가 실행되지 않는 것**이 문제다. doctor의 값어치는 새 지식이 아니라 "사람이 대조하지 않아도 되게 만드는 것"에 있다.

**초안 중 발견 (설계를 바꾼 실측)** — 설치본 `0.12.0 (a117486)` vs 마켓플레이스 `0.12.0 (3267b82)`. **버전 문자열은 같은데 sha는 뒤처져 있다.** 스킬 문면이 범프 없이 들어가면 "최신인데 낡음"이 생기므로, 버전 문자열만 대조하는 진단은 그 경우 **조용히 틀린다** — doctor가 없애려던 바로 그 실패 양상이다.

## 2. 목표 / 비목표

**목표**

1. **C-9** — 환경 4항목을 일괄 진단하는 읽기 전용 스킬 `doctor` 신설. 명시 호출과 **증상 문장** 양쪽에서 뜬다.
2. **C-6 D11** — setup 마커 템플릿의 "큰 변경" 문구가 경량 트랙을 배제하는 것처럼 읽히는 문제 해소.
3. **fp-I3** — dev-cycle description의 파이프라인 요약이 G-c(트리거-only)를 위반하는 소지 해소. **조건부** — §3 참조.

**비목표**

- **자동 수정** — 설치본 갱신·인증·설정 쓰기 어느 것도 하지 않는다. 진단 후 기존 경로로 위임한다.
- **다른 머신 진단** — doctor는 실행되는 머신 하나만 본다. 3머신 정렬은 "각 머신이 각자 돌린다"로 달성한다.
- **스크립트·훅 추가** — 산출물은 `SKILL.md` 1개다. 프로브가 한두 줄이라 스크립트는 과설계다(G1·G2).
- **review-loop 실행 중의 codex 실패 처리** — review-loop 소관이다(`review-loop:159·271`).
- **C-4(핸드오프 표준화)** — 순번 7c.
- **fan-out 리포트 파일화 규약** — 순번 미배치 백로그.

## 3. 설계 (D1~D21 확정 — harden-spec 2026-08-09 · D22·D23 = spec 루프 batch 판정 2026-08-09)

### C-9. doctor 신설

**성질** — 진단 전용. **사용자 작업물·설정을 쓰지 않는다**(D9). 이름은 codex CLI의 `codex doctor`와 같은 어휘를 쓴다(사용자가 뜻을 추측할 필요가 없다).

> **D9 — "읽기 전용"의 정확한 범위.** 점검 2의 `git fetch`는 엄밀히 `.git` 객체를 갱신하므로 "파일을 쓰지 않는다"는 초안 문면과 어긋났다. 실제 경계는 **사용자 작업물·설정(repo 파일·CLAUDE.md·settings.json·설치본)을 쓰지 않는다**이며, 마켓플레이스 clone의 `fetch`는 **진단에 필수인 조회 행위로 예외 명시**한다. `fetch`는 상위 clone의 작업 트리를 바꾸지 않고 원격 추적 단서만 갱신하므로 실질 위험이 없다. fetch 없이 현 clone 상태로 판정하면 상시 "판정 불가"가 나온다 — 실측에서 로컬 clone HEAD(`a117486`)가 origin/main(`3267b82`)보다 뒤처져 있었다.

`SKILL.md` frontmatter에 **`model`·`effort` 오버라이드를 두지 않는다**(D21) — 명시된 프로브를 돌려 표로 정리하는 낮은 난이도 작업이라 harden-spec 같은 모델 고정의 근거(놓친 갭 하나가 재설계를 부름)가 없고, 고정하면 가벼운 진단에 비싼 모델을 강제하게 된다.

**세 스킬의 경계**

| 스킬 | 성질 | 발동 |
|---|---|---|
| `dev-cycle` | 읽기 전용 — **파이프라인** 지도 | 새 기능·다음 단계 질문 |
| **`doctor`** | 읽기 전용 — **환경** 진단 | 명시 호출 + 증상 문장 |
| `setup` | **쓰기** — repo 채택 | 명시 요청 전용 |

**점검 4항목** (프로브는 SKILL.md에 그대로 명시한다 — 매 실행 재조합 금지)

| # | 항목 | 프로브 | 이상 판정 |
|---|---|---|---|
| 1 | 설치본 버전 | `installed_plugins.json`의 dev-workflow **엔트리 배열을 전부 열거** → 엔트리마다 `scope`·(있으면) `projectPath`·`version`·`gitCommitSha` | 마켓플레이스보다 뒤처짐 · 미설치 · **스코프 간 드리프트**(판정 방법 = 규칙 (a) 하위 — 트리 비교, raw sha 비교 금지) |
| 2 | 마켓플레이스 최신 | 마켓플레이스 clone `fetch` → `dev-workflow/` **subtree object id** 대조(설치sha vs `origin/main`), 차이가 있을 때만 `git log … -- dev-workflow/`로 내역 표시 | subtree가 다르면 뒤처짐 · fetch/조회 실패 = **"판정 불가"** · clone 없음 = **"마켓플레이스 미등록"**(D19) |
| 3 | codex | `codex login status` + CLI 존재 + companion 스크립트 존재 — **이 3종까지만**(D15) | 미로그인 · CLI 없음 · companion 없음. **3종이 전부 정상인데도 문제가 있으면 `codex doctor`로 위임** |
| 4 | `CLAUDE_CTX_LIMIT` | **실제 환경변수 값** + settings.json 3곳(글로벌·프로젝트·로컬) **병행**(D10) + **현재 모델 대조** | **모델↔유효 limit 불일치를 양방향으로 본다** — ① 200k 모델인데 미설정·1M 설정 = 과소평가로 **넛지 미발화** ② 1M 모델인데 200k 설정 = 과대평가로 **조기 넛지** |

**판정 규칙 5건**

- **(a) 버전 뒤처짐은 `dev-workflow/` 경로 한정으로 판정한다**(D5). 설치 sha와 `origin/main`의 **`dev-workflow/` 내용이 같은지**만 본다.
  - **판정은 커밋 이력이 아니라 트리로 한다.** 1차 판정 = 양쪽 sha의 `dev-workflow/` **subtree object id 비교**(`git rev-parse <sha>:dev-workflow`). 같으면 내용이 동일하므로 **뒤처짐 없음/드리프트 없음**이다. 다를 때만 `git log … -- dev-workflow/`를 **무엇이 달라졌는지 보여주는 보조 근거**로 붙인다. object·경로 조회가 실패하면 (b)에 따라 **"판정 불가"**다.
  - **왜 로그로는 안 되나.** `git log A..B -- <path>`는 *그 경로를 건드린 커밋을 세는 것*이지 최종 트리를 비교하지 않는다. 변경 후 되돌린 이력이 있거나 서로 다른 커밋이 같은 내용을 만들면 **트리는 같은데 로그는 남아** 거짓 뒤처짐·거짓 드리프트가 된다 — (a)가 없애려던 늑대소년을 한 겹 아래에서 재생산하는 것이다.
  - **근거(실측 2026-08-09).** user-scope 설치본 `a117486` vs `origin/main` `3267b82` 사이에 `dev-workflow/` 변경이 **0건**이다 — sha는 다른데 플러그인 내용은 동일하다. 이 repo는 docs/specs 커밋이 압도적이라 **단순 sha 비교는 커밋할 때마다 거짓 경보**한다. 늑대소년이 되면 진짜 뒤처짐도 무시된다 — doctor가 없애려던 실패 양상을 반대 방향으로 재생산하는 것이다.
  - 이 방식은 §1이 지적한 **"버전 문자열은 같은데 내용은 낡음"**(범프 없는 스킬 문면 변경)도 그대로 잡는다. version 문자열은 표시용이지 판정 근거가 아니다.
  - 현 상태 판정: user-scope = **최신**(sha 상이하나 경로 변경 0건), project-scope `d57bfe9`(0.11.0) = **뒤처짐 3건**.
  - **스코프 간 드리프트도 같은 방식으로 판정한다.** 설치 sha들을 raw로 비교하지 않고 **각 sha의 `dev-workflow/` subtree object id가 모두 같은지**만 본다. 이 규칙이 없으면 (a)가 origin 비교에서 막은 오탐이 **스코프 상호 비교로 그대로 새어 들어온다** — 이 repo는 docs/specs 커밋이 압도적이라 두 스코프가 같은 내용으로 갱신돼도 raw sha 비교는 영구히 드리프트를 보고한다(반례: `d23f2bb`·`a117486`·`3267b82`는 sha가 다르지만 `dev-workflow/` 트리가 같다). 어느 한쪽 sha가 결측이거나 조회가 실패하면 (b)에 따라 **"판정 불가"**다.
  - **스코프 목록을 하드코딩하지 않는다.** 설치 스코프는 `user`·`project`·`local` 3종이고(`claude plugin install -s <scope>`), `installed_plugins.json`의 엔트리는 `scope`와 (project·local이면) `projectPath`를 가진 **배열**이다. 두 종만 열거하면 **stale한 local 설치가 활성인 repo에서 doctor가 그 설치를 보지 못한 채 "최신"이라 보고한다** — 조용히 틀리는 바로 그 실패다. 그래서 배열 전체를 열거하고 스코프 이름은 **출력에 그대로 옮긴다**(새 스코프가 생겨도 자동으로 덮인다).
- **(b) 근거가 없으면 "판정 불가"로 표시하고 절대 "최신"이라 말하지 않는다.** 네트워크 실패뿐 아니라 **`gitCommitSha`·`version` 필드 결측**에도 같은 규칙을 적용한다(**D11** — D6의 확장).
  - **근거(실측).** `installed_plugins.json`에 `gitCommitSha`가 아예 없는 엔트리가 **5건**(claude-md-management · security-guidance · ralph-loop · kotlin-lsp · swift-lsp), `version: "unknown"`이 **3건** 있다. dev-workflow 자신은 현재 둘 다 있지만 구조적으로 보장되지 않는다. version 문자열로 폴백하면 (a)가 막은 "버전 같은데 내용 다름"이 다시 새어 들어온다.
- **(c) 마켓플레이스 clone 부재는 "미등록"으로 별도 표시한다**(D19). 네트워크 실패와 **원인도 조치도 다르다** — 전자는 기다림, 후자는 `/plugin marketplace add gguii74-rwk/claude-dev-workflow` → `/plugin install`이다(setup 마커 블록의 기존 문구 재사용). 브리프 `:149`의 "그램 최초 install 백로그 잔존"이 정확히 이 케이스다.
- **(d) `CLAUDE_CTX_LIMIT`은 실제 환경변수 값을 먼저 본다**(D10). 훅이 실제로 읽는 것은 `env.CLAUDE_CTX_LIMIT` — **프로세스 환경변수**다(`context-threshold-hook.mjs:45`). 셸 프로필 등 다른 경로로 주입되면 settings.json grep만으로는 "미설정" 오탐이 난다. settings.json 3곳은 **"어디서 왔는가 / 어디에 넣으면 되는가"**를 밝혀 조치 안내를 붙이는 용도로 병행 조회한다.
- **(e) 모델 대조는 doctor만 할 수 있고, 불일치는 양방향으로 판정한다.** 훅은 런타임에 윈도를 모르지만 **스킬은 Claude 자신이 자기 모델을 안다.** 훅이 구조적으로 못 하는 검사를 메우는 것이며, setup과 중복이 아니다 — setup은 값을 **묻고 쓰는** 쪽, doctor는 **현재 모델과 맞는지 판정하는** 쪽이다.
  - **양방향인 이유(실측).** 훅은 설정된 양수 값을 **검증 없이 그대로** 쓴다(`context-threshold-hook.mjs:52` — `Number.isFinite(envLimit) && envLimit > 0 ? envLimit : DEFAULT_LIMIT`). 그래서 **1M 모델에 200k가 설정된 역방향**도 실패한다: 실제 8% 사용 시점을 40%로 계산해 멀쩡한 작업에 조기 넛지가 뜬다. 훅 자신이 그 방향을 *"과대평가가 더 해롭다 — 1M 세션에 200k를 가정하면 실제 8% 사용 시점에 40% 임계로 오판해 멀쩡한 작업을 조기 종료시킨다"*(`:50~51`)고 적어 뒀다. 한 방향만 규정한 진단은 **훅이 스스로 더 해롭다고 적은 쪽을 못 잡는다.**

**버전 대조 범위 = dev-workflow 하나뿐이다**(D12). codex는 점검 3의 **존재·인증만** 보고 플러그인 버전은 대조하지 않는다 — 비목표 "다른 머신 진단 안 함"과 같은 결의 범위 제한이며, 상류 마켓플레이스(`openai/codex-plugin-cc`) fetch를 추가하지 않아 산출물을 `SKILL.md` 1개로 유지한다.

**출력** — **정상이든 아니든 4항목 표를 항상 출력**하고(D16), 조치 안내만 이상 항목에 붙인다. 증상 트리거로 뜬 경우 사용자는 이미 문제를 의심하는 중이라 "이상 없음" 한 줄로는 납득되지 않고, 무엇을 봤는지 안 보이면 결국 수동 대조로 되돌아간다 — 그것이 doctor가 없애려던 바로 그 행동이다. 표 4줄은 짧다.

**오발동 경계 4종** (문면에 명시)

| 잠식 위험 | 경계 |
|---|---|
| review-loop | **루프 실행 중의 codex 실패는 review-loop 소관.** doctor가 끼면 루프가 끊긴다. doctor는 **루프 밖**의 증상 문장에만 |
| setup | "이 repo에 세팅해줘"는 setup. 쓰기가 필요하면 setup으로 보낸다 |
| dev-cycle | "다음 단계 뭐야"는 dev-cycle. doctor는 환경만 |
| 막연한 실패 문장 · **타 제품 문맥** | "왜 안 돼?"에 뜨면 안 되고, **다른 제품·도구를 주어로 한 같은 어휘**("playwright 플러그인 최신인가")에도 뜨면 안 된다 — **D14의 어휘 규칙**으로 구분한다 |

**D14 — 증상 어휘의 경계선** (**D22로 정정 — 2026-08-09 사용자 결정**). description에는 **dev-workflow 파이프라인 환경 요소(넛지 · codex 연동 · 플러그인 버전)를 가리키는 증상 문장만** 올린다 — "넛지가 안 떠" · "codex가 안 돌아" · "플러그인 최신인가" · "버전 맞나". **문면에 명시적으로 제외**를 적을 대상은 둘이다: ① 도구 이름·현상에 묶이지 않은 일반 실패 문장("왜 안 돼", "에러 났어", "환경이 꾸직해") ② **다른 제품·도구를 주어로 한 같은 어휘**("playwright 플러그인 최신인가", "node 버전 맞나", "codex로 딴 작업 돌리는데 안 돌아").

> **왜 규칙 문면을 넓혔나(D22).** 초안 문면은 *"주어가 dev-workflow 고유 명사인 문장만"*이었는데 예시 4종 중 3종에는 제품 귀속이 없다 — 규칙과 예시가 어긋난 채로는 구현자가 어느 쪽을 따를지 갈린다. 예시를 규칙에 맞춰 좁히는 길(모두 "dev-workflow …"로 시작)은 **사용자가 실제로 그렇게 말하지 않아** 증상 트리거의 값어치를 없애고 D1의 2경로를 명시 호출 하나로 수렴시킨다. 그래서 **규칙을 실제 의도(파이프라인 환경 요소)에 맞춰 정정하고, 넓어진 만큼 잠식은 제외 ②와 음성 arm 실측으로 받는다.** 경계를 문면에 두는 이유는 측정 때문이다 — §5의 오발동 음성 arm이 측정할 대상이 문면에 없으면 측정 자체가 성립하지 않는다. 넓히면 발견성은 오르지만 상위 스킬·일반 디버깅과 겹쳐 잠식이 커지고, 이는 spec이 "오발동은 이 스킬의 주된 위험"으로 본 판단과 정면으로 어긋난다.

**dev-cycle 본문에는 doctor 상호참조를 넣지 않는다**(D20). 발동 경로를 **명시 호출 + 증상 트리거** 둘로 한정한 것이 기결정(D1)이며, dev-cycle 경유는 제3 경로 신설이라 기결정 확장이다. 세 스킬 경계표(파이프라인 vs 환경)의 선명함도 유지되고, §5 fp-I3 회귀 arm이 같은 파일을 측정 중이라 arm 정의가 흐려지지 않는다. 상호참조는 AC 5의 README 트러블슈팅으로 충족한다.

### C-6 D11. setup 마커 문구

현재 템플릿은 *"새 기능·**큰 변경**을 시작하면"* 이라 경량 트랙을 배제하는 것처럼 읽힌다. dev-cycle description은 이미 "작은 단일 단계 변경도 경로 판정이 필요하면"을 담고 있어 **둘이 어긋나 있다.** 마커 문구를 dev-cycle 트리거에 맞춰 정합시킨다.

**파생 문제 — 기존 삽입분 드리프트 (D13으로 해소).** 마커는 이미 여러 repo의 CLAUDE.md에 삽입돼 있고, setup은 멱등 교체라 **다시 돌려야** 갱신된다.

**D13 — 9단계 종료 보고에 안내 한 줄로 닫는다.** 릴리스 안내에 *"마커가 삽입된 repo는 `/dev-workflow:setup` 재실행으로 문구를 갱신한다"*를 넣는다. 기계 장치를 만들지 않으므로 G1·G2(과설계 금지)와 정합하고, 마커는 dev-cycle로 가는 **포인터**라 문구가 낡아도 기능 자체는 깨지지 않는다 — 드리프트의 실제 손해가 "경량 트랙 배제처럼 읽힘"에 국한되므로 안내로 충분하다. doctor의 5번째 점검 항목으로 넣는 안은 기각했다: 환경 진단이 아니라 repo 콘텐츠 검사라 doctor의 성질과 결이 다르다.

### fp-I3. dev-cycle description — 조건부 결정

`(design → spec → harden → review → plan → review → implement → review → integrate)` 괄호가 G-c(description은 트리거만)를 위반할 소지다. 다만 C-6 ledger가 *"W1이 5/5 선택으로 발견성 목적은 이미 달성"* 이라 기록해 뒀다 — **제거하면 발견성이 떨어질 수 있고 그게 유일한 쟁점**이다.

**결정: 제거를 시도하고 발견성 TDD로 판단한다.** 5/5가 유지되면 제거 확정, 떨어지면 **유지하고 D번호로 예외를 기록**한다. 실측에 달린 항목을 미리 못 박지 않는다.

## 4. 현행 접점 — 개정 지도

| 파일 | 변경 |
|---|---|
| `skills/doctor/SKILL.md` | **신설** (C-9) |
| `skills/setup/SKILL.md` | 마커 템플릿 문구 1곳 (D11) |
| `skills/dev-cycle/SKILL.md` | description 괄호 요약 (fp-I3, 조건부) |
| `.claude-plugin/plugin.json` | description에 doctor 추가 + **0.13.0** 범프 |
| **`.claude-plugin/marketplace.json`** (repo 루트) | **초안 누락 — harden-spec에서 보강.** plugin 항목의 description에도 스킬 목록(`dev-cycle · harden-spec · ui-mockup · review-loop · writing-plans-split · setup`)이 하드코딩돼 있어 doctor를 넣지 않으면 마켓플레이스 목록에서만 낡은 소개가 남는다 |
| `README.md` · `README.ko.md` · `README.ja.md` | 사용법 섹션 추가 + 목차 + 상단 스킬 표 + 기존 트러블슈팅에서 상호참조 |

`skills/` 디렉터리는 자동 발견이므로 plugin.json에 스킬 목록을 등록할 필요는 없다. **dev-cycle은 description만 고친다** — 본문은 건드리지 않는다(D20).

## 5. 검증 계획

**writing-skills TDD** — arm 3종 `new`(개정) / `cur`(현행) / `none`(무지침 대조군). 무지침 대조군은 7a에서 **규정 왜곡 1건**을 실제로 잡아냈으므로 유지한다.

| 계열 | 프롬프트 | arm | 런 |
|---|---|---|---|
| 발견성(양성) | 증상 문장 4종 — "넛지가 안 떠" · "codex가 안 돌아" · "플러그인 최신인가" · "버전 맞나" | **`new` 단독**(D17) | 20 |
| **오발동(음성)** | setup·dev-cycle이 떠야 하는 문장 + 루프 중 codex 실패 + **D14 제외 ①**(막연한 실패 문장 — "왜 안 돼" · "에러 났어") + **D14 제외 ②**(타 제품 문맥 — "playwright 플러그인 최신인가" · "node 버전 맞나" · "codex로 딴 작업 돌리는데 안 돌아") | `new`만 | **40** |
| fp-I3 회귀 | C-6의 W1 프롬프트 재사용 — 요약 제거 후 5/5 유지되는가 | new·cur·**none** | **15** |
| D11 | 작은 변경 문장에서 마커를 읽고 dev-cycle로 가는가 | new·cur·**none** | **15** |

**총 90런**(D23) — 7a(68런)와 같은 구간이며 C-6이 경고한 243런 구간에 들어가지 않는다.

**D23 — `none` 대조군은 기존 문면 개정 계열에만 배정한다** (2026-08-09 사용자 결정). §5는 무지침 대조군을 "유지한다"고 선언했는데 초안 예산은 전부 new·cur에 배정돼 **none 0건이어도 GREEN**이었다. 다만 doctor 계열(발견성·오발동)에서는 `none`도 `cur`도 **doctor가 없는 상태**라 D17과 같은 이유로 정보가 0이다. 그래서 none이 실제로 정보를 주는 **fp-I3·D11(기존 문면 개정)** 두 계열에만 5런씩 배정해 총 90런으로 올린다. **none 결과는 양성 합격 기준과 분리된 대조 진단으로만 기록한다** — 무지침 상태와 비교해 개정 문면이 행동을 왜곡하지 않는지 보는 용도이며(7a에서 규정 왜곡 1건을 실제로 잡았다), GREEN/RED를 가르지 않는다.

**D17 — 양성 계열은 `new` 단독 절대기준으로 본다.** 초안의 "`cur` 대비 분리 입증"은 **검증력이 0**이었다: `cur`(현행 0.12.0)에는 doctor 스킬 자체가 없어 **뜨는 것이 원리적으로 불가능**하므로 항상 0/5이고 분리는 자동 성립한다. spec 자신이 음성 arm을 `new`만 도는 근거로 *"cur에는 doctor가 없어 잠식 자체가 불가능"*이라 적었는데, 같은 논리가 양성에도 그대로 적용된다. 7a는 기존 스킬 **개정**이라 cur 비교가 의미 있었지만 7b는 **신설**이다. 비는 20런은 **오발동 음성 arm으로 재배분**한다 — spec 스스로 "오발동은 이 스킬의 주된 위험"으로 판단했으므로, 정보가 0인 곳에서 진짜 위험으로 예산을 옮기는 것이다. fp-I3·D11 계열은 **기존 문면 개정**이라 cur 비교가 유효하므로 그대로 둔다.

**판정 기준 (확정)**

- **양성 = `new` 5/5 절대기준.** 증상 문장 4종 각각에서 doctor가 뜬다. 미달이면 description 어휘를 고쳐 재측정한다.
- **음성 = 잠식 0.** `new`에서 setup·dev-cycle·review-loop가 떠야 할 문장, D14 제외 ①(막연한 실패 문장), D14 제외 ②(타 제품 문맥)에 doctor가 뜬 횟수 = **음성 40런 전체에서 0건**. **1건이라도 뜨면 GREEN이 아니다** — 오발동은 이 스킬의 주된 위험이라 양성보다 엄격하게 본다.
- **fp-I3 회귀 = 5/5 유지**(`new` 기준). 미달이면 제거를 철회하고 유지로 판정한다(§3 조건부 결정).
- **`none` 대조군 = 합격/불합격을 가르지 않는다**(D23). fp-I3·D11 계열의 none 결과는 **무지침 대비 행동 왜곡이 있는지**를 보는 대조 진단으로 기록만 하고, 왜곡이 관측되면 그 자체를 finding으로 올려 별도 판정한다.
- **진단 정확도 = 대조표 전항 일치**(D18). 아래 참조.

**D18 — 진단 정확도 실측을 GREEN 조건에 넣는다.** TDD 4계열은 전부 *"doctor가 뜨는가"*만 측정하고 **프로브가 내린 판정이 맞는지는 아무도 보지 않는다.** 초안 그대로면 틀린 판정을 내는 doctor도 GREEN으로 릴리스된다 — 실제로 D5가 고친 sha 오탐이 초안·brainstorming을 통과해 harden-spec까지 살아온 것이 반례다. 그래서 구현 후 **doctor를 이 머신에서 실제로 돌려** 아래 대조표가 전부 일치하는지 확인하고 결과를 impl ledger에 적는다. D18이 더하는 것은 **수동 실측 2회**(200k 모델 세션 1회 + 1M 모델 세션에서 `CLAUDE_CTX_LIMIT` 주입 1회)뿐이다 — TDD 런 수(D23 기준 90)와는 별개이고, 기계 장치를 만들지 않으므로 경량 경로·G1·G2에 저촉하지 않는다.

| 대조 항목 | 기대 판정 |
|---|---|
| user-scope (`a117486`) | **최신** — sha가 origin/main과 달라도 `dev-workflow/` 변경 0건 |
| project-scope (`d57bfe9`, 0.11.0) | **뒤처짐** — 경로 변경 3건 |
| **스코프 간 드리프트** | 실제 스코프 sha 쌍에 대해 **doctor와 다른 명령**(`git diff --quiet <shaA> <shaB> -- dev-workflow/`의 종료코드)으로 낸 정답과 doctor 판정이 **일치**. doctor가 쓰는 `rev-parse … :dev-workflow`를 oracle로 재사용하지 않는다 |
| `CLAUDE_CTX_LIMIT` 과소평가 방향 — **200k 모델 세션** | **이상** — 미설정이라 기본 1M으로 계산돼 넛지 미발화 |
| `CLAUDE_CTX_LIMIT` 과대평가 방향 — **1M 모델 세션** | **이상** — `CLAUDE_CTX_LIMIT=200000` 주입 시 조기 넛지 |
| codex | **정상** — `Logged in using ChatGPT`(exit 0), companion 존재 |

**오탐 케이스는 자연 발생 여부를 확인한 뒤 기록한다.** 구현이 갈리는 지점은 둘이다 — ① raw sha 비교와 트리 비교가 갈리는 **"sha는 다른데 `dev-workflow/` 트리는 같은 쌍"**, ② 로그 기반과 트리 기반이 갈리는 **"경로를 건드린 커밋은 있는데 최종 트리는 같은 쌍"**(변경 후 되돌린 이력). ①은 V-7b-1 갱신 직후에 성립할 수 있으나 그 시점 origin/main 위치에 달려 있고, ②는 이 repo 이력에 그런 쌍이 있어야 한다. **성립하는 것만 실측하고, 성립하지 않으면 그 사실을 impl ledger에 적고 규칙 문면 확인으로 갈음한다.** 설치본을 조작하거나 픽스처용 커밋을 만들지는 않는다(D9·G1·G2).

**local 스코프는 이 머신에 설치본이 없다.** 열거 방식으로 고쳤으므로 코드 경로는 스코프 종류에 의존하지 않지만, local 설치의 실측은 이 머신에서 불가능하다 — **자연 픽스처 부재를 impl ledger에 명시**하고, 열거가 배열 전체를 도는지(스코프 이름 하드코딩이 없는지)를 문면·구현 확인으로 갈음한다.

**CTX_LIMIT 두 방향은 한 세션에서 동시에 성립하지 않는다.** 미설정을 "과소평가 이상"으로 판정하려면 현재 모델이 **200k**여야 하는데, 같은 200k 세션에 `CLAUDE_CTX_LIMIT=200000`을 주입하면 그건 **정상**이다. 반대로 1M 세션에서는 미설정이 정상이고 주입만 이상이다. 그래서 두 행은 **서로 다른 모델 세션**에서 재고(`/model` 전환), **각 실행의 모델을 impl ledger에 함께 적는다.** 한 세션에 몰면 AC 8은 *충족 불가능한 게이트*이거나 *틀린 doctor도 통과시키는 게이트*가 된다. 환경변수는 실행 1회에 앞에 붙이는 것으로 충분해 파일을 쓰지 않으므로 D9 범위를 유지한다. 판정 규칙 (e)를 양방향으로 고쳤으니 검증도 양방향이어야 한다 — 새 규칙에 D18의 취지(*"프로브가 내린 판정이 맞는지 아무도 보지 않는다"*)가 미치지 않으면 고친 의미가 없다.

**픽스처** — 발견성·오발동 계열은 사용자 발화만 주면 되므로 환경을 망가뜨릴 필요가 없다(7a보다 부담이 작다). 진단 정확도(D18) 쪽은 이 머신이 **이상 상태 2종을 자연 픽스처로 갖고 있다** — ops-hub project-scope 0.11.0 고정 · `CLAUDE_CTX_LIMIT` 미설정(**200k 모델 세션에서 이상이 된다** — 1M 세션에서는 기본값과 일치해 정상이다). 설치본을 조작하지 않고 실측되며, 남는 것은 모델 세션 전환과 환경변수 1회 주입뿐이다.

**예외 — D11 계열은 픽스처가 필요하다.** "작은 변경 문장에서 마커를 읽고 dev-cycle로 가는가"를 측정하려면 **개정된 마커 블록이 삽입된 CLAUDE.md**가 arm별로 있어야 한다(`new`=개정 문구, `cur`=현행 "새 기능·큰 변경" 문구). 발화만으로는 측정되지 않으므로 이 10런은 CLAUDE.md 픽스처를 준비한다.

**7a 교훈 승계** — 픽스처는 호출부 연결로 끝내지 말고 **게이트와 base diff까지 실측 통과**시킨다. 같은 계열 결함이 C-5·C-6·7a로 4회 반복됐다.

**프로브 사전 실증(2026-08-09)** — 4종 전부 한두 줄로 동작함을 확인했다: 설치본 스코프별 조회 · 마켓플레이스 fetch + `origin/main:plugin.json` · `codex login status`(→ `Logged in using ChatGPT`, exit 0) · settings.json 3곳 grep.

**8단계 게이트** — 비-npm repo이므로 **writing-skills TDD GREEN 기록으로 갈음**한다(C-6 D7).

## 6. 완료 조건 (acceptance criteria)

1. `skills/doctor/SKILL.md` 신설 — 진단 전용(D9 범위) · 프로브 4종 명시 · **판정 규칙 5건**(경로 한정 비교 — **`dev-workflow/` subtree 비교로, origin 대비 + 스코프 상호 둘 다** · 근거 없으면 판정 불가 · 마켓플레이스 미등록 · CTX_LIMIT 실값 우선 · **모델↔limit 양방향 대조**) · **설치 엔트리 배열 전체 열거**(스코프 이름 하드코딩 없음) · 출력 규격(**항상 표**) · 오발동 경계 4종 + **D14/D22 증상 어휘 목록과 제외 문장 2종**(막연한 실패 문장 · 타 제품 문맥) · frontmatter에 model/effort 오버라이드 **없음**.
2. setup 마커 문구가 dev-cycle 트리거와 정합(C-6 D11).
3. dev-cycle description — fp-I3 조건부 판정이 TDD 결과로 닫히고 근거가 D번호로 기록됨. **본문은 미변경**(D20).
4. `plugin.json` **0.13.0** 범프 + description에 doctor 반영.
5. **`marketplace.json`** plugin description에 doctor 반영.
6. README 3종 동기 갱신 — 사용법 섹션 · 목차 · 상단 스킬 표 · 트러블슈팅 상호참조.
7. writing-skills TDD **GREEN** — 4계열 **90런**(D23). **양성 = `new` 5/5 절대기준**, **음성 = 40런 전체에서 잠식 0건**(1건이라도 뜨면 실패), **fp-I3 = 5/5 유지**. **fp-I3·D11 계열의 `none` 5런씩은 실행·기록이 필수이나 합격 기준은 아니다**(대조 진단). 기준 상세는 §5.
8. **진단 정확도 실측 GREEN**(D18) — **6항 대조표**가 전부 일치하고 **각 실행의 모델과 함께** impl ledger에 기록됨. `CLAUDE_CTX_LIMIT` 두 방향은 **200k 세션 1회 + 1M 세션 1회**로 나눠 재고(§5), 나머지 4항은 어느 실행에서 봐도 된다. TDD GREEN만으로는 이 AC를 대신할 수 없다.
9. `review-loop --phase spec` · `--phase impl` **미판정 blocking 0**으로 종결.
10. 9단계 — 릴리스 0.13.0 origin 반영 + 각 머신 `/plugin update` 안내 + **마커 삽입 repo의 setup 재실행 안내**(D13) + **V-7b-1 잔여분**(project-scope가 0.13.0까지 도달하는지 릴리스 후 확인). 종료 보고에 **다음 순번 = 7c(C-4)** 명시.

## 7. 초안 미해결 질문 — 전건 해소 (harden-spec 2026-08-09)

| Q | 해소 |
|---|---|
| **Q1** 마커 드리프트 | **D13** — 9단계 종료 보고 안내로 닫는다(기계 장치 없음) |
| **Q2** 증상 어휘 경계선 | **D14** — 주어가 dev-workflow 고유 명사인 문장만. 제외 문장을 문면에 명시 |
| **Q3** "판정 불가" 문면 | 기결정(초안 6)의 표현 문제 — 판정 규칙 (b)로 흡수. 근거 결측·네트워크 실패를 같은 어휘로 표기하고 **"최신"이라는 단어를 쓰지 않는다** |
| **Q4** project-scope 조치 | **조회로 해소.** project-scope 설치는 **repo의 `.claude/settings.json`**(`extraKnownMarketplaces` + `enabledPlugins`)이 트리거다(ops-hub 실측). 갱신 명령은 user-scope와 같고 **실행 위치가 다르다** — 그 repo 안에서 `/plugin update`를 돌려야 한다. 안내 문구는 이 차이를 명시한다 |
| **Q5** codex 위임 경계 | **D15** — 신호 3종(CLI 존재·`login status`·companion 존재)까지만 보고, 3종이 정상인데도 문제면 `codex doctor`로 넘긴다 |
| **Q6** 자기참조 | **조회로 해소.** 0.12.0 이하 설치본에는 doctor가 **존재하지 않아** 애초에 뜨지 않는다. 문제는 0.13.0 **이후** 버전 간에만 성립하며, 그때도 D5의 경로 한정 비교는 설치본 sha와 원격만 쓰므로 **자기 문면의 최신 여부와 무관하게 동작**한다 |
| **Q7** 정상 출력 | **D16** — 항상 표를 출력한다 |

## 8. 승계 · 이월

- **C-6 D11**(setup 마커 문구) · **fp-I3**(dev-cycle description) → **이 트랙에서 종결**한다. 7a가 비목표로 넘긴 2건이다.
- **C-4(핸드오프 표준화)** → **순번 7c**(2026-08-09 사용자 결정). 출발점 = `2026-08-09-ledger-handoff-mockup-nudge.md` §부록.
- **C-10** → 순번 8. 착수는 7c 이후.
- **실측 유예 2건(V1·V2)** — 0.11.0 배포로 관찰 창이 열렸다. 이 트랙의 blocking이 아니다.
- **repo 밖 후속(C-6 D17, 미처리)** — ops-hub의 impl ledger 위치 분열 2건. 처리 여부는 사용자 판단.

## 재논의 금지(기결정)

> **D번호 표기 규칙** — 아래 `D<n>`은 **이 문서의 로컬 번호**다(harden-spec 2026-08-09 부여 D1~D21, spec 루프 batch 판정 D22·D23). 이전 트랙 결정을 가리킬 때는 `C-6 D11`처럼 **접두를 붙인다** — 접두 없는 `D11`은 이 문서의 로컬 결정(필드 결측 판정 불가)이고, setup 마커 문구 결정은 항상 `C-6 D11`로 쓴다.

**승계 기결정 (재론 금지 — 이 spec의 전제):**

- **브리프 §기결정 대조** — G1·G2(**기계 검증 장치 과설계 금지**) / ESCALATE batch 유지 / **description은 트리거만**(G-c) / no-AI-trace.
- **사용자 결정** — 순번 1~8 순차 진행(2026-08-07, 순번 8은 2026-08-09) / 순번 7의 7a·7b 분할(2026-08-09) / **C-4 = 순번 7c**(2026-08-09) / **이 트랙은 경량 경로 — 4단계 수행, 5·6단계 생략**(2026-08-09, 세션 한정 승인).
- **C-6 D7** — 비-npm repo의 8단계 게이트는 그 repo 검증 명령 또는 writing-skills TDD GREEN 기록으로 갈음.
- **C-6 D17 / 7a C-3** — planless 트랙의 impl ledger = spec 문서 말미.
- **3.5(ui-mockup) 비대상** — 화면을 만들지 않는다(스킬 문면). ui-mockup 규약상 생략 기록도 남기지 않는다.

**이 트랙의 확정 결정 (D1~D23 — 전건 사용자 결정 또는 사용자 확인):**

| D | 결정 | 출처 |
|---|---|---|
| **D1** | doctor 발동 = **명시 호출 + 증상 트리거** | 초안(사용자) |
| **D2** | doctor 조치 = **진단 전용 + 위임** | 초안(사용자) |
| **D3** | 버전 비교 = **이 머신 vs 마켓플레이스 최신**. 다른 머신은 진단하지 않는다 | 초안(사용자) |
| **D4** | 구현 형태 = **스킬 문면에 조회 명령 명시**(A안). 스크립트·훅 추가 없음 | 초안(사용자) |
| **D5** | 버전 뒤처짐 판정 = **`dev-workflow/` 경로 한정 비교**. 단순 sha 비교는 상시 오탐하므로 폐기 | harden-spec |
| **D6** | 근거가 없으면 **"판정 불가"** — "최신"이라 말하지 않는다 | 초안(사용자) |
| **D7** | `CLAUDE_CTX_LIMIT` 점검 근거 = **모델 대조**. 훅이 구조적으로 못 하는 검사이며 setup과 중복이 아니다 | 초안(사용자) |
| **D8** | fp-I3은 **조건부** — TDD 결과로 제거/유지가 갈린다 | 초안(사용자) |
| **D9** | "읽기 전용"의 범위 = **사용자 작업물·설정을 쓰지 않는다**. 마켓플레이스 clone `fetch`는 진단 필수 조회로 **예외** | harden-spec |
| **D10** | `CLAUDE_CTX_LIMIT` 프로브 = **실제 환경변수 값 + settings.json 3곳 병행** | harden-spec |
| **D11** | `gitCommitSha`·`version` **필드 결측**도 "판정 불가"(D6 확장). version 문자열 폴백 금지 | harden-spec |
| **D12** | 버전 대조 범위 = **dev-workflow 하나뿐**. codex는 존재·인증만 본다 | harden-spec |
| **D13** | 마커 드리프트 = **9단계 종료 보고 안내**로 닫는다(기계 장치 없음) | harden-spec |
| **D14** | 증상 어휘 = **주어가 dev-workflow 고유 명사인 문장만**. 막연한 실패 문장은 문면에 명시 제외 | harden-spec |
| **D15** | codex 위임 = **신호 3종까지만**. 그 이상은 `codex doctor` | harden-spec |
| **D16** | 출력 = **항상 4항목 표**. 조치 안내만 이상 항목에 | harden-spec |
| **D17** | 양성 arm = **`new` 단독 절대기준**. cur 대비 분리는 신설 스킬에서 검증력 0이므로 폐기, 남는 20런은 음성 arm으로 재배분 | harden-spec |
| **D18** | **진단 정확도 실측**을 GREEN 조건에 포함. TDD GREEN으로 대신할 수 없다 (실행 횟수는 대조표 항목 수에 따르는 구현 세부라 결정 내용이 아니다) | harden-spec |
| **D19** | 마켓플레이스 clone 부재 = **"미등록"** 별도 상태 + `marketplace add` 안내 | harden-spec |
| **D20** | dev-cycle **본문에 doctor 상호참조를 넣지 않는다**(D1의 2경로 유지) | harden-spec |
| **D21** | doctor frontmatter에 **`model`·`effort` 오버라이드 없음** — 세션 모델을 쓴다 | harden-spec |
| **D22** | 증상 어휘 = **dev-workflow 파이프라인 환경 요소(넛지·codex 연동·플러그인 버전)를 가리키는 문장**. D14의 "주어가 고유 명사" 문면을 정정하고, 제외에 **② 타 제품·도구 문맥의 같은 어휘**를 추가해 음성 arm으로 실측한다 | **spec 루프 batch 판정** |
| **D23** | `none` 대조군은 **fp-I3·D11 계열에만 5런씩**(총 90런). 신설 doctor 계열은 none ≡ cur라 정보 0. none 결과는 **합격 기준이 아닌 대조 진단**으로 기록 | **spec 루프 batch 판정** |

## 잔여 리스크 (DEFERRED)

**DEFERRED = 0.** 판단 갭 전건이 질문으로 닫혔고 사용자가 유예한 항목이 없다.

**검증 필요(실측) 1건** — 조회로 결정되지 않고 사용자가 결정할 수도 없는 외부 사실이라 구현/전환 단계 실측으로 넘긴다.

| # | 항목 | 왜 지금 못 닫나 | 언제 닫나 |
|---|---|---|---|
| V-7b-1 | **project-scope 캐시가 실제로 갱신되는 명령** | 설치 기전(repo `.claude/settings.json` 트리거)은 조회로 확인했으나, `/plugin update`는 슬래시 커맨드라 CLI로 실행·관측할 수 없다. "그 repo 안에서 실행하면 project-scope 캐시가 갱신된다"는 구조적 추론이며 미실측이다 | **D18 대조표 확인 직후** — 검증 대상은 *"그 repo 안에서 돌리면 project-scope 캐시가 갱신되는가"*이지 특정 버전 도달이 아니므로, **그 시점에 이미 origin/main에 있는 배포본(0.12.0)으로 갱신되는지**를 본다. 갱신되지 않거나 다른 위치를 요구하면 조치 안내 문구를 고친다 |

**V-7b-1의 실행 순서 — 릴리스보다 앞설 수 없는 부분을 분리했다.** 0.13.0은 8단계 GREEN·impl 리뷰를 거쳐 **9단계에서야** origin에 올라가므로(§상단 경로 순서), "0.11.0→0.13.0" 확인은 사전 게이트에서 원리적으로 불가능하다 — `/plugin update`가 가져올 0.13.0이 아직 원격에 없다. 그래서 **명령의 유효성(0.12.0으로 갱신)은 D18에서 사전 검증**하고, **0.13.0 도달 확인은 9단계 릴리스 후 안내**(AC 10)에 붙인다. 또 이 갱신은 D18 대조표 2행의 자연 픽스처(project-scope `d57bfe9` 0.11.0 = 뒤처짐)를 **소모**하므로, 반드시 **대조표를 먼저 기록한 뒤** 실행한다.

## 적대검증 ledger (spec)

- 기준: base `3267b82`(트랙 시작 직전) → target HEAD. 적대 라운드 상한 5 · 확인 라운드 예산 2 · 자동 모드 3라운드.
- **이 표가 단일 원본이다** — 다른 문서로 복제하지 말고 참조만 한다.

| fp | fingerprint (file · title · recommendation 요지) | severity | disposition | 근거 · 상태 |
|---|---|---|---|---|
| **fp-S1** | 이 spec · *D14를 위반하는 양성 arm이 오발동을 GREEN으로 만든다* · 양성 문장을 dev-workflow에 명시 귀속시키고 타 제품 문맥을 음성 arm에 추가 | medium | **FIXED** (batch 판정 → **D22**) | R1 지목 · R2 재출현(2회차) → 자동 모드 종료 시 batch 일괄 제시. **사용자 판정 = "규칙 문면을 넓히고 제외·음성 보강"**(2026-08-09). D14 규칙을 *파이프라인 환경 요소*로 정정, 제외 ② (타 제품 문맥) 신설, 오발동 경계표 4행 확장, §5 음성 arm에 타 제품 문맥 프롬프트 추가. **부수 정정 1건**: 음성 판정 기준의 분모가 40런 계열인데 `0/10`으로 남아 있어 "40런 전체에서 0건"으로 바로잡음. **미확인 FIXED 큐 잔존** |
| **fp-S2** | 이 spec · *CTX_LIMIT의 역방향 불일치를 정상으로 통과시킨다* · 모델↔유효 limit 불일치를 양방향으로 정의하고 실측에 반영 | medium | **FIXED** (R1) | 훅이 설정 양수 값을 검증 없이 사용(`context-threshold-hook.mjs:52`)하고 주석 `:50~51`이 그 방향을 *"과대평가가 더 해롭다"*고 명시 → 실재 갭. §3 점검표 4번 · 판정 규칙 (e) · §5 D18 대조표 · AC 1 · AC 8 반영. **미확인 FIXED 큐 잔존** · 적대 비재출현(R2) · **후속 결함 fp-S4 파생** |
| **fp-S3** | 이 spec · *0.13 project-scope 갱신 검증이 릴리스 전에 실행될 수 없다* · 사전 검증 가능한 형태로 재배치하거나 post-release 게이트로 이동 | medium | **FIXED** (R1) | 0.13.0은 9단계에서야 origin에 오르므로 D18 시점에 `/plugin update`가 가져올 대상이 없다 → 실행 불가 확인. 검증 명제를 "명령 유효성(0.12.0 갱신)"과 "0.13.0 도달"로 분리해 전자는 D18, 후자는 AC 10으로 배치. 픽스처 소모 순서도 명시. **미확인 FIXED 큐 잔존** · 적대 비재출현(R2) |
| **fp-S4** | 이 spec · *양방향 CTX_LIMIT 실측의 두 기대값이 한 모델에서는 동시에 성립하지 않는다* · 두 실행을 200k·1M 세션으로 분리하고 각 실행의 모델을 기록 | medium | **FIXED** (R2) | **fp-S2 수정이 만든 결함**(R2 지목). 200k 세션은 미설정=이상·주입=정상, 1M 세션은 그 반대라 5항 표를 한 세션에서 채울 수 없다 → AC 8이 충족 불가 게이트가 된다. §5 대조표를 모델 세션별로 분리하고 AC 8을 6항 + 모델 병기로 개정. **미확인 FIXED 큐 잔존** |
| **fp-S5** | 이 spec · *스코프 간 드리프트가 경로 한정 규칙 없이 별도 이상으로 남아 있다* · 드리프트를 두 설치 sha의 `dev-workflow/` 트리 차이로 정의하고 결측·조회 실패는 판정 불가 | medium | **FIXED** (R2) | D5는 origin 대비만 규정해, 같은 오탐이 스코프 상호 비교로 새어 든다. spec 자신의 실측(`a117486`↔`3267b82` = sha 상이·트리 동일)이 반례. 규칙 (a)에 하위 항목 추가 + 점검표 1번 이상 판정에 판정 방법 명시 + D18 대조표에 드리프트 오탐 행 추가. **미확인 FIXED 큐 잔존** · **후속 결함 fp-S7 파생** |
| **fp-S6** | 이 spec · *지원되는 local 설치 스코프를 진단에서 누락한다* · 엔트리 전체를 열거하고 적용 projectPath를 표시 | medium | **FIXED** (R3) | 실측: `claude plugin install -s`는 `user`·`project`·`local` 3종을 지원하고 `installed_plugins.json` 엔트리는 `scope`·`projectPath`를 가진 배열이다 → "user + project" 하드코딩은 사실 오류. 점검표 1번 프로브를 **배열 전체 열거**로 교체 + 규칙 (a)에 하드코딩 금지 항 추가 + AC 1 반영. **local 픽스처는 이 머신에 없어 문면·구현 확인으로 갈음**(설치본 조작 안 함 — D9·G1·G2). **미확인 FIXED 큐 잔존** |
| **fp-S7** | 이 spec · *스코프 드리프트 명령이 최종 트리 차이를 판정하지 못한다* · subtree object id 비교로 교체하고 oracle을 구현과 분리 | medium | **FIXED** (R3) | **fp-S5 수정이 만든 결함**. `git log A..B -- <path>`는 경로를 건드린 **커밋을 세는 것**이라 변경→revert 이력에서 트리가 같아도 로그가 남아 거짓 뒤처짐·거짓 드리프트가 된다. 판정을 **`rev-parse <sha>:dev-workflow` subtree 비교**로 바꾸고 로그는 보조 근거로 강등, 점검표 2번 프로브·규칙 (a)·D18 oracle(`git diff --quiet`, 구현과 다른 명령) 반영. **미확인 FIXED 큐 잔존** |
| **fp-S8** | 이 spec · *유지한다고 명시한 무지침 대조군에 실행 예산이 없다* · `none` 실행 수를 표·판정 기준·AC 7에 명시하고 총 런 수 동기화 | medium | **FIXED** (batch 판정 → **D23**) | §5가 `none` arm을 "유지한다"고 선언했으나 80런은 new·cur에만 배정돼 있고 AC 7도 그 80런만 요구 → none 0건이어도 GREEN. D17(양성 arm의 cur 재배분) 재론이 아니라 별개 공백. **사용자 판정 = "fp-I3·D11 계열에 5런씩 추가 → 총 90런"**(2026-08-09). §5 표·총런·판정 기준·AC 7 동기화. **미확인 FIXED 큐 잔존** |

**라운드 기록**

| 라운드 | 모드 | verdict | 신규 finding | score(수정 전) | 미확인 FIXED 큐 |
|---|---|---|---|---|---|
| R1 | 적대 | needs-attention | medium 3 (critical/high/low 0) | **3** | 2 (fp-S2 · fp-S3) |
| R2 | 적대 | needs-attention | medium 2 신규 + fp-S1 재출현 (critical/high/low 0) | **3** | 4 (fp-S2 · fp-S3 · fp-S4 · fp-S5) |
| R3 | 적대 | needs-attention | medium 3 신규 (critical/high/low 0) | **4** | 6 (+ fp-S6 · fp-S7) |

**전환 신호 ① 발화 (R3)** — score `3 → 3 → 4`로 `s_n ≥ s_{n-1} ≥ s_{n-2}` 성립. 동시에 적대 소진 3 = `--auto-rounds` 도달이라 자동 모드도 종료다. 이후 순서 = **batch ESCALATE 일괄 제시 → 확인 모드 진입**(적대 라운드는 더 돌리지 않는다. `--max 5`는 남았으나 신호가 우선한다).

**batch flush (2026-08-09, 자동 모드 종료 시점)** — 적재분 2건을 자동 수정 내역과 함께 일괄 제시했고 사용자가 전건을 닫았다.

| 항목 | 사용자 판정 | 결과 |
|---|---|---|
| fp-S1 | 규칙 문면을 넓히고 제외·음성 보강 | **D22** 신설, FIXED로 반영 |
| fp-S8 | fp-I3·D11 계열에 none 5런씩 → 총 90런 | **D23** 신설, FIXED로 반영 |
| 자동 수정 6건(커밋 `75feac4`·`df7d414`·`98a1f3a`) | 전부 승인 | 롤백 없음, 확인 모드 진행 |

batch 큐는 이 시점에 **비었다**. 미판정 blocking 0 · 미확인 FIXED 큐 8건으로 확인 모드에 진입한다.

- score 산식 = `critical4·high3·medium1`, 스냅샷은 §2c 분류 직후·수정 전(FIXED 후보 + 미해결 ESCALATE 포함, 미확인 FIXED 큐 제외).
- 기결정 가드 대조 결과: R1 3건 모두 D1~D21·승계 기결정의 **재론이 아니라 문면·검증계획 내부 모순** 지적이라 DUPLICATE 해당 없음. 루프 직접 판정(ACCEPTED/OUT_OF_SCOPE/DEFERRED_TO_IMPL/DUPLICATE) **0건**.
