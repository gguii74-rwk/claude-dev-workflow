# ui-mockup 스킬 설계 (UI/UX 목업 선택 단계)

- 상태: 설계 승인됨 (brainstorming 산출물, 구현 전)
- 대상: `dev-workflow` 플러그인에 추가할 **신규** 스킬 `ui-mockup` (파이프라인 3.5단계, 옵션)
- 날짜: 2026-07-25

## 1. 배경 / 문제

현행 파이프라인(brainstorming → spec → harden-spec → review-loop → plan → review-loop → 구현 → review-loop)에서 **화면(UI/UX)이 필요한 기능은 사용자 의사와 무관하게 구현이 진행**되고, 기능 완성 후 UI/UX를 다시 개선하는 재작업이 반복된다. 취향·시각 결정이 파이프라인 어디에서도 명시적으로 수집·기록되지 않기 때문이다.

## 2. 목표 / 비목표

**목표:** 화면이 새로 생기거나 구성이 바뀌는 기능에서, plan 이전에 사용자의 시각적 취향을 목업 선택으로 확정하고 **spec에 기결정으로 기록**해 구현 단계의 임의 재해석을 차단한다. 선택 결과를 **프로젝트 스타일 가이드로 수렴**시켜 기능 간 UI 파편화를 막는다.

**비목표 (YAGNI):**
- 스크린샷 자동 비교(playwright 대조) — impl 검증은 수동 대조 수준.
- 인터랙션 프로토타입 — 목업은 정적 HTML.
- 스택별 컴포넌트 코드 생성 — 목업은 HTML/CSS 중립, 구현 계약은 시각·구성이지 코드가 아니다.
- 디자인 토큰 자동 주입 도구.
- UI가 없는 기능에의 강제 — 트리거 미충족 시 완전히 건너뛴다.

## 3. 파이프라인 내 위치

```
… → harden-spec → [ui-mockup (옵션, 3.5)] → review-loop(spec) → plan → …
```

**harden-spec 직후·review-loop(spec) 직전**인 이유:
- harden-spec이 화면 목록·빈 상태·에러 상태·범위를 굳힌 **뒤**라 목업 재작업이 최소화된다(상태 확정 전에 만들면 구조적으로 다시 만든다).
- 선택된 UI 결정이 spec에 반영된 **뒤** codex 적대검증(review-loop spec)을 통과하므로, 목업발(發) spec 변경도 검증망 안에 있다.
- review-loop(spec) 이후에 두면 굳은 spec을 목업 선택이 다시 바꿔 재검증 루프가 꼬인다.

**트리거 (관찰 가능 판정):** "spec이 새 화면을 만들거나 기존 화면 구성을 바꾸는가." 예 → 진입 확인 후 실행, 아니오 → 건너뜀. 판정·안내 지점은 harden-spec 종료 권고와 dev-cycle 판별 로직(§6).

## 4. 동작 설계 (5단계)

### 입력
- 굳은 spec 경로(생략 시 harden-spec과 동일한 자동 탐색 + 확인).
- `docs/design/style-guide.md` 존재 여부로 발산 모드 결정.

### 절차

**1) 로드.** spec에서 대상 화면·핵심 상태(기본/빈/에러) 목록을 추출하고 style-guide.md 존재를 확인한다.

**2) 발산 (가이드 수렴형).**
- **가이드 없음(첫 실행):** 서로 다른 스타일 **4종** 목업.
- **가이드 있음:** 가이드 준수 **레이아웃 변형 2~3종**만(스타일 재발산은 사용자가 명시 요청할 때만).
- 목업은 자체완결 HTML(외부 리소스 없음), **서브에이전트 병렬 생성**. 생성 에이전트는 `model: fable` 지정(시각 품질 — 본체는 세션 모델). 설치돼 있으면 frontend-design·dataviz 스킬을 생성 에이전트가 참조.
- 각 목업은 기본 화면 + 핵심 상태 1~2개(빈/에러)를 포함한다 — harden-spec이 굳힌 상태가 시각화되는 지점.

**3) 제시·선택.** 탭/분할로 한눈에 비교하는 **비교 페이지 HTML**을 함께 생성해 전달하고, 선택은 AskUserQuestion으로 받는다. **조합 지정 허용**("2번 레이아웃 + 4번 색") — 조합이면 조합본 1회 재생성 후 확정한다(재조합 무한 루프 금지, 추가 조정은 수정 요청으로).

**4) 확정·기록.**
- 선택본을 `docs/design/<feature>/`에 정리(후보·비교 페이지 포함 커밋 — 이력 보존).
- **첫 실행이면 선택 결과에서 `docs/design/style-guide.md`(색·타이포·간격·컴포넌트 규칙)를 추출·생성**한다.
- spec에 `## UI 설계` 섹션(선택 목업 경로 + 핵심 결정 요약)을 추가하고, **`## 재논의 금지(기결정)` 블록에 UI 결정을 기록**한다 — review-loop·plan·구현의 재해석을 기결정 가드로 차단(harden-spec 규약과 동일 메커니즘).

**5) 종료.** design 파일 + spec만 명시 stage 커밋(`git add -A` 금지, AI 서명 금지). review-loop(spec)을 권고하고 정지(자동 전환 금지 — 파이프라인 규약 동일).

## 5. 하지 말 것 (red flags 후보 — 구현 시 확정)

- 트리거 미충족(UI 없음)인데 목업을 만든다 → 건너뛴다.
- 가이드가 있는데 스타일 4종을 재발산한다 → 레이아웃 변형만(재발산은 명시 요청 시).
- 선택을 안 받고 "가장 나은" 목업을 스스로 확정한다 → 선택은 AskUserQuestion, 판정자는 사용자.
- UI 결정을 spec에 기록하지 않고 목업 파일만 남긴다 → 재논의 금지 블록까지가 산출물.
- 목업에 외부 CDN·리소스를 넣는다 → 자체완결 HTML.

## 6. 연동 변경 (각 한 곳)

| 대상 | 변경 |
|---|---|
| dev-cycle | 파이프라인 표에 3.5 옵션 행 추가 + 판별 로직에 "spec이 화면을 만들거나 바꾸고 docs/design/<feature>가 없다 → 3.5 ui-mockup" 조건 추가 |
| harden-spec | 종료 권고에 조건부 한 줄: "spec이 새 화면/구성 변경을 포함하면 review-loop 전에 ui-mockup 권고" |
| review-loop | impl 체크리스트에 "구현 화면이 `docs/design/<feature>/` 선택 목업의 레이아웃·스타일 가이드를 따르는가"(수동 대조) 항목 추가 |
| plugin.json | 0.7.0 범프, description에 ui-mockup 추가 |
| README ×3 | 도구 표·사용법에 ui-mockup 절 추가 |

## 7. 성공 기준 (acceptance criteria)

- UI 없는 spec에서는 실행되지 않는다(트리거 판정).
- 첫 실행: 4종 발산 → 선택(조합 포함) → style-guide.md 생성 → spec에 UI 설계 + 재논의 금지 기록 → 커밋 → 정지.
- 가이드 존재 시: 레이아웃 변형 2~3종만 생성한다(스타일 재발산 없음).
- 선택 없이 목업이 확정되는 경로가 없다(writing-skills 베이스라인 테스트로 검증).
- review-loop(impl)가 목업 대조 항목을 체크리스트에서 인지한다.

## 8. 구현 노트

- 파일: `dev-workflow/skills/ui-mockup/SKILL.md` (+ §6 연동 4곳).
- description은 트리거 조건만 서술(워크플로 요약 금지 — writing-skills 원칙). 트리거 예: "목업 만들어줘", "UI 시안 보여줘", "화면 구성 정하자".
- 스킬 본문 한국어, "Announce:" 도입부 — 기존 스킬 스타일 준수.
- 구현은 writing-skills TDD 규약(베이스라인 → 개정 → 검증) 준수 — harden-spec 0.6.0 개정과 동일 방식.

## 9. 미해결 질문

없음. (스킬 이름 `ui-mockup`은 구현 시 이견 없으면 확정.)
