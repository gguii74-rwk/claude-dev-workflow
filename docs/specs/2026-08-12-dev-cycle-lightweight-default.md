# dev-cycle 경량 분기 기본값 재설계 — 확인 불가 폴백 (H2 / A/B 권고 1)

- 단계: **7 구현 종결 (2026-08-13, writing-skills TDD GREEN — §7단계 구현 기록) — 다음 = 8 impl 적대검증**(새 세션, 게이트 = TDD GREEN 기록 갈음, ledger 위치 = 이 spec 말미). 5·6 생략 확정(D13). 3.5 비대상(화면 무관 — 스킬 문면 변경).
- 출처: A/B 실측 파일럿 권고 1 (`~/workspace/dev-workflow-eval/report/AB-2026-08-12.md` §5) — H2 채택(전 rep 일관). 교훈 원칙 L3("기본값은 싼 쪽", `LESSONS-2026-08-12.md`).
- 대상: `dev-workflow/skills/dev-cycle/SKILL.md` (0.16.0 기준 96줄) — 경량 경로 절(세션 상태 판정 문장) + 가드 3(세션 상태 재확인).

## 1. 문제 (실측 근거)

현행 문면은 경량 진입을 사용자 승인에만 걸어 둔다 — "승인했을 때만 경량으로 진입한다 — 거절·무응답·모호한 답은 승인이 아니므로 정식을 유지한다"(판정 문장) · "미확인 기본값 = 정식"(계층 표·가드 3). 질문을 전달할 수 없는 운용(질문 금지 계약의 서브에이전트·자동 배치)에서는 승인 자체가 불가능하므로 **경량 태스크도 구조적으로 전부 정식**이 된다.

A/B 실측(2026-08-11~12, 35런, 블라인드 판정): 경량층(L)은 **전 arm 만점(17/17 pass) — 성과 이득 0** 인데, 0.16.0 비용은 none 대비 **시간 ×26**(2.4분 → 63.6분)·**토큰 ×6.3**(33k → 209k). 최악 L1-r1 = 98분·12라운드·282k(×41) — 오탈자 4개 수정에 spec→plan→impl 3단 루프 발동. 한계 §4-3: 실사용자라면 "경량으로 가자"고 답했을 상황이지만, **기본값이 정식인 한 자율 운용에서 비용 폭발은 구조적으로 재현된다.**

## 2. 목표 / 범위 / 비목표

**목표** — 확인 불가 상황의 경량/정식 판정이 사용자 승인 대신 **접촉 표면 스캔 결과를 채택**하게 해, 자율 운용의 경량 태스크 비용 폭발을 제거한다. 대화형 확인 1회와 안전 바닥(하한 3종·즉시 승격·fail-closed)은 유지한다.

**범위** — `dev-cycle/SKILL.md` 단일 파일, 세 지점:
- 경량 경로 절에 **확인 불가 폴백 소절 신설**(정의·판정 규칙·감사 기록 — §3a).
- 판정 문장에 참조 1구(무응답을 거절 나열에서 분리해 폴백으로 보냄).
- 가드 3(세션 상태 재확인)에 참조 1구(준용).
- **줄 수 예산(C-6 D15)**: 순증 후 총량 **≤100줄(하드)**. 현행 96줄이므로 실질 순증 여유 +4줄 — 소프트 95 초과분은 D15 압축 순서(부연·예시 → 중복 서술)로 상쇄를 시도하고, 이식 항목은 압축하지 않는다.

**비목표** —
- **가드 1(fail-closed) 무변경** — 문서 신호 불명확은 정확성 축이며 비용 축이 아니다(D2).
- **하한 3종·즉시 승격·접촉 표면 목록 불변** — 하한 완화는 소멸률 리스크 축이라 별도 실측 없이는 규약 완화 불가(L1). §7 미해결 질문에 후속 후보로만 기재.
- **자율 운용에서 하한 3종의 수행 방식은 비대상** — 확인 불가 문맥에서 3 harden-spec(사람 적대 단계) 등을 어떻게 수행하는가는 기존 규약(하네스 계약의 서술 갈음 등) 그대로 두며, 폴백은 판정만 바꾼다.
- **경량 진입 후 4·5·6 생략 선택 규약 무변경** — 생략은 여전히 단계별 선택 + spec D기록(C-6 D2). 자동 판정이 생략 패키지를 만들지 않는다.
- 트리비얼 탈출구("파이프라인 비대상" 기준선) 신설 없음 — 새 판단 표면을 만들지 않는다(D2).
- review-loop 등 타 스킬 문면 무변경 · 기계 장치(파서·스크립트) 도입 금지(C-6 G-b) · description/트리거 변경 없음.

**릴리스 표면(구현 단계 — D5·G-f 운용화)** — plugin.json `0.17.0` 범프(릴리스 직전 선점 재확인, 선점 시 차기 minor) · README 3종(en/ko/ja)의 경량 승인 문단 동기화(현행 "미확인 기본값 정식" 공개 문구에 부분 대체 반영) · 3머신 `/plugin update` 안내. SKILL.md 단일 파일 한정은 **행동 규칙 변경**의 범위다 — 릴리스 표면은 여기에 별도 명시한다.

## 3. 설계

### 3a. 확인 불가 폴백 소절 (문면 초안 — 구현에서 최종 확정, D15 예산 내 압축 허용)

> **확인 불가 폴백** — 확인 불가는 **명시 신호 2종**에만 성립한다: ① 질문을 전달할 수 없는 운용 계약(질문 금지 계약의 서브에이전트·자동 배치·cron·CI 등 비대화 실행) ② 사용자의 부재·자율 진행 명시("묻지 말고 진행해", "돌려놓고 퇴근할게"). 대화형에서 답이 아직 안 온 상태·거절·모호한 답은 확인 불가가 아니다(정식 유지). 성립 시 **접촉 표면 스캔**(변경 요청 문면 + 접촉할 파일·모듈의 표면, grep 수준 조회)으로 판정한다: **명확히 무신호면 경량, 신호가 있거나 스캔이 불명확하면 정식**(fail-closed). 자동 판정으로 경량 진입 시 spec에 1줄을 남긴다(spec이 아직 없으면 최초 작성 시 포함): `경량 자동 판정 (<날짜>): 확인 불가(신호 ①|②) · 접촉 표면 무신호`. (실측: A/B 2026-08-12 H2 — 확인 불가 기본값 정식이 경량 태스크에 시간 ×26 세금)

- 입증 인용은 서술형 관례(fp-I17 D9와 동형) — 규칙 문면에는 실측 요지만, 파일 포인터는 이 spec에만.
- **감사 기록의 역할 = 감사 + 과거 판정의 판독 원본(D11)** — 판별·가드가 "왜 경량으로 진행됐나"를 질문 없이 읽는다. 판정 기록이며 **생략 기록이 아니다**(4·5·6 생략은 별도 D기록 — 현행 규약). **기록은 재스캔을 갈음하지 않는다** — 확인 불가 재개는 기록 유무와 무관하게 폴백을 재적용해 재스캔으로 재판정하고(스캔은 grep 수준으로 싸다 — 재개 사이 같은 파일에 생긴 위험 신호를 stale 기록이 덮는 구멍 원천 차단), **대화형 재개는 기록과 무관하게 확인 1회**(존속 규칙 "대화형 확인 1회"·"승인 세션 한정 수명" 집행). 판독 불가·불완전 기록은 감사 가치만 잃는다(판정은 어차피 재스캔). 접촉 표면 절대 규칙·즉시 승격(§3c)은 상시 우선한다.
- **기록 주체·영속화** — 기록은 **폴백 판정을 소비해 경량으로 진입하는 세션**이 남긴다(승격 사유 기록과 동일 관례 — spec 부재 시 최초 작성 시 포함). **dev-cycle 지도 판별 자체는 파일을 쓰지 않는다**(G-g 유지) — 판별만 하고 진입 없이 세션이 끝나면 기록 의무도 발생하지 않는다. 영속화 전 유실은 안전 방향이다 — 재개 세션이 기록 부재로 폴백을 재적용한다(fail-closed).

### 3b. 판정 문장·가드 3 수정

- 판정 문장: "승인했을 때만 경량으로 진입한다 — 거절·**모호한 답**은 승인이 아니므로 정식을 유지한다. **확인 자체가 불가하면(§확인 불가 폴백) 폴백을 적용한다.**" (현행의 "무응답"을 거절 나열에서 빼 폴백으로 보낸다 — 거절·모호한 답은 응답이 존재하므로 폴백 비대상.)
- 가드 3: "…그 세션 1회 확인" 뒤에 "**확인 불가면 확인 불가 폴백 준용**" 1구. 준용 = 재개 세션도 확인 불가면 **기록 유무와 무관하게 재스캔으로 재판정**(기록은 재스캔을 갈음하지 않는다 — §3a D11).
- (8단계 R1 반영) 폴백 소절에 **적용 범위 한정 문장** 1개: 폴백이 갈음하는 확인 = **세션 상태(경량/정식) 확인 + 가드 3 재확인뿐** — 가드 1 fail-closed 확인·9단계 머지/배포 승인 등 다른 사용자 확인은 확인 불가여도 스캔으로 대체되지 않는다(미완 취급 보류·이월). 근거 = impl ledger R1(범위 미명시 시 LLM 독자가 폴백을 외부 승인까지 확장 — GREEN 출력 실증 3/12).

### 3c. 경계 (무변경 확인)

- 접촉 표면 절대 규칙("아래에 닿으면 정식이다")은 그대로 — 폴백은 그 규칙의 **적용 주체를 사용자 확인에서 스캔으로 바꿀 뿐**이다. 스캔의 판정 주체 = 지도 1차 판정과 동일 주체(별도 장치 없음).
- 오판 보험 = 기존 즉시 승격 규칙(진행 중 표면 발견 → 그 자리에서 정식 + 사유 기록) 무변경.
- 대화형 세션의 확인 1회 유지 — 응답 가능하면 묻는 쪽이 싸다(질문 1회 ≪ ×26).

## 4. 결정사항 (D1~D10 — 아래 재논의 금지 블록이 정본)

브레인스토밍(D1~D5)·harden-spec(D6~D10) 사용자 판정 2026-08-12. 상세는 §재논의 금지.

## 5. Acceptance Criteria

- **AC1 (경량 자동 판정)**: 경량 태스크 + 확인 불가 픽스처 — **유형 ①(계약형)·②(부재 선언형) 각각**에서 new: 경량 자동 판정 + 감사 기록 1줄(신호 유형 표기 포함, 전건). cur: 정식 발동(RED — A/B L층 실측으로 재현성 기확보).
- **AC2 (과소 판정 회귀 대조군)**: 접촉 표면 신호 + 확인 불가 — **유형 ①·② × 입력원 2종(위험 신호가 요청 문면에만 / 요청은 무해하고 접촉할 파일·모듈 표면에만) 각각**에서 new도 전건 정식(경량 오판 0 — 후자 입력원은 파일 표면을 실제로 읽어야만 통과하므로 D8 두 입력원 계약을 강제. ②의 "묻지 말고 진행"이 표면 판정을 누르는 회귀 검출 포함). **스캔 불명확 픽스처(대상 파일 부재·조회 실패·상충 증거) — new도 전건 정식(fail-closed 검증)**.
- **AC3 (대화형 무변경 대조군)**: 응답 가능 문맥에서 — new도 확인 1회(질문 생략 0). 대화형 무응답 대기 픽스처는 정식 유지(폴백 오발동 0).
- **AC4 (가드 3 준용·기록 역할)**: 재개 + 확인 불가 픽스처 — **기록 유무와 무관하게** 폴백 재적용(재스캔 기준 판정. 재개 사이 위험 신호가 유입된 경우 정식 — stale 기록 맹신 0). 기록 실재 + 현재 대화 가능 재개 — 확인 1회(기록의 질문 우회 0). 기록 실재 재개에서 과거 경량 진행 사유의 재질문 0(판독 원본 역할). **활성 4·5·6 생략 D기록이 있는 재개 + 위험 신호 유입 — 정식 판정에 더해 승격 전이 3종 전건(승격 사유 기록 · 미수행 생략 기록 철회 · 첫 미완 단계 재계산 — 기존 즉시 승격 계약 준수, dev-cycle 지도 무수정 유지)**.
- **AC5 (공정·릴리스)**: 스킬 문면 변경 = writing-skills TDD(C-6 G-e) · no-AI-trace(G-d) · 기계 장치 0(G-b) · 총량 ≤100줄(D15) · **경량 자동 판정 런에서 dev-cycle 지도 판별의 파일 무수정(G-g — 기록 주체 = 진입 세션)** · **릴리스 표면 완료(§2 릴리스 표면 — plugin.json 범프 + README 3종 동기화 + 3머신 안내)**.
- **AC6 (폴백 적용 범위 — 8단계 R1 신설)**: 확인 불가 + repo 규약 부재(가드 1 대상·9단계 외부 승인) — new: 폴백·스캔이 세션 상태 판정·가드 3 재확인 **밖**의 확인을 갈음하지 않음(무확인 머지·배포 자체 결정 0, 해당 항목 미완 취급·이월) + 경량 진입 유지(AC1 비회귀). cur(범위 한정 문장 부재 문면): 확장 해석 재현(RED).

## 6. TDD 계획 (개요 — 배치 설계는 7단계에서 확정)

- 확인 불가 재현: **유형 ① = 질문 금지 RUN 계약**(fp-I17 harness-8 방식 재사용 — fp-I17 D13 동형) · **유형 ② = 프롬프트에 부재·자율 진행 지시 1문장 이식**. 셀: (a1) 경량+① (a2) 경량+② (b1) 표면 신호+① (b2) 표면 신호+② — b계열은 각각 **입력원 2종 분리**(요청 문면만 위험 / 파일·모듈 표면만 위험, AC2) (c) 대화형(무응답 대기 포함) (d) 재개(감사 기록 유/무 × 확인 불가/대화형 + 재개 전 위험 신호 유입 — 확인 불가는 전건 재스캔, 대화형은 확인 1회 기대 + **활성 생략 D기록 재개는 승격 전이 3종을 합격 조건으로**, AC4) (e) 스캔 불명확(파일 부재·조회 실패·상충 증거 → 정식) (f — 8단계 R1 신설) 폴백 범위: A1·A2 픽스처 재사용(R-lt의 CLAUDE.md/AGENTS.md 부재 = repo 규약 부재 내장), 판정 = AC6 경계 기준(기대 정답 사전 등록 = `.remember/tdd9-fix-plan.md`, RED는 기존 GREEN 출력 재판정으로 갈음). 케이스별 독립 5런 하한(fp-S10 관례), cur/new 양 arm. **(a1)(a2) 판독에 관찰 지표(합부 아님, D12)를 병기한다: 생략 선택·D기록 성립 여부 + 실수행 단계 수 — §7 후속 트랙(생략 실행률) 근거용.**
- 실행 시점: 스펙·설계까지 이 세션(사용자 지시) — TDD 배치는 토큰 사용량 보고 후 계속/8-17 이후 중 결정.

## 7. 미해결 질문

- **하한 3종의 경량 잔여 비용** — 경량 판정 후에도 오탈자류에 spec 문서+harden-spec+impl 적대검증이 남는다(×26 중 폴백이 못 줄이는 몫). 완화는 소멸률 실측이 선행돼야 하는 별도 트랙(권고 2·XVAL §7 감량 방향과 얽힘). 이 트랙 비대상. **R3 적대검증도 같은 축의 갭을 지적(경량 라벨 ≠ 생략 실행 — 자율 운용에서 4·5·6 생략 선택이 성립하지 않으면 비용이 남는다). 종단 간 비용 AC는 신설하지 않기로 사용자 판정(D12) — §6 관찰 지표가 생략 실행률 데이터를 수집해 이 후속 트랙의 근거가 된다.**
- ~~"질문 금지 계약" 문구의 포괄성~~ → **해소(D7)**: 확인 불가 = 명시 신호 2종 한정, 대화형 무응답 비대상.

## 잔여 리스크 (DEFERRED)

- **DEFERRED(사용자 유예): 없음** — 이번 압박의 판단 갭 6건 전건 질문·확정(D6~D10, G3은 기결정 적용).
- **검증 필요(실측, 구현 단계): 없음** — 확인 불가 유형 ①·②의 판정 동작은 TDD 셀이 직접 측정(§6). 하네스 재현성은 fp-I17 전례로 기확인.

## 재논의 금지(기결정)

> 표기: 접두어 없는 **D<n>은 이 트랙(H2)의 결정**이다. 타 트랙 결정은 접두어로 명시한다(C-6 D<n>·C-10 D<n>·fp-I17 D<n>).

**승계 기결정 (재론 금지)** — C-6 **G-b**(기계 장치 금지·신호는 grep 수준) · **G-d**(no-AI-trace) · **G-e**(스킬 문면 변경 = writing-skills TDD 필수) · **G-f**(릴리스 규약) · **G-g**(dev-cycle 읽기 전용 지도) · **G-h**(단계 경계 = 새 세션) · **D2**(생략은 단계별 선택+D기록) · **D3**(하한 3종 불가침) · **D4**(승격 즉시) · **D15**(SKILL.md 하드 100줄·소프트 95줄·압축 순서) · C-10 **가드 체계**(가드 1 fail-closed · 가드 2 비소급 결과 기준 · 계층 표 구조) · fp-I17 **D9**(입증 인용 서술형) · **D10**(릴리스 버전 선점 시 차기 minor) · **D13**(하네스 재사용 우선).

**부분 대체 선언** — **계열 A 재결정·C-6 D5의 "미확인 기본 정식"은 확인 불가 부분집합(§3a 명시 신호 2종)에 한해 이 트랙으로 대체된다.** 근거: A/B 2026-08-12 H2 실측(×26) — 원 기결정(2026-08-10) 이후의 신규 실측이며, 사용자 재판정 2026-08-12(트랙 차터 + D1). **존속**: 승인 세션 한정 수명 · 대화형 확인 1회 · 거절/모호한 답 = 정식. C-10 spec 원본은 불가침(역사 기록) — 대체 선언의 거처는 이 spec 한 곳이다.

**이번 트랙 확정 (2026-08-12 사용자 판정)**:

| # | 결정 | 근거 |
|---|---|---|
| D1 | 폴백 구조 = **무응답 폴백만 교체** — 대화형 확인 유지, 확인 불가 시에만 자동 판정, 스캔 불명확 → 정식(fail-closed 잔존) | 응답 가능하면 확인 1회가 싸고, 비용 폭발은 자율 운용에서만 구조적(A/B 한계 §4-3) |
| D2 | 범위 = **판정 폴백만** — 가드 1·하한 3종·생략 선택 규약 무변경, 트리비얼 탈출구 없음 | 가드 1은 정확성 축. 하한 완화는 소멸률 실측 선행 필요(L1) |
| D3 | 문면 = **폴백 소절 신설 + 참조 2곳** | "확인 불가" 정의 단일화(거울면 금지 정합) |
| D4 | 자동 판정 시 **spec 1줄 감사 기록 의무** | 감사 + 과거 판정의 판독 원본(문서 기록 계층). 원 근거 "가드 3 재확인 대체"는 D11이 대체 — 기록은 재스캔을 갈음하지 않는다 |
| D5 | 릴리스 = **0.17.0**(선점 시 차기 minor — fp-I17 D10 준용), 3머신 `/plugin update` 안내 | 릴리스 규약(G-f) |
| D6 | 계열 A·C-6 D5 **부분 대체 명시**(위 선언 블록) | codex 재지목 선제 차단 + 대체 계보 기록 |
| D7 | 확인 불가 = **명시 신호 2종 한정**(① 질문 금지·비대화 운용 계약 ② 사용자 부재·자율 진행 명시), 대화형 무응답 대기·거절·모호 답 비대상 | "무응답 잠깐 → 경량" 루프홀 차단, TDD 재현 가능한 경계 |
| D8 | 스캔 정의 = **변경 요청 문면 + 접촉할 파일·모듈 표면, grep 수준 조회**, 판정 주체 = 지도 1차 판정 주체 | G-b 정합(기계 장치 없음), 요청만 스캔하면 접촉 표면 누락 |
| D9 | 감사 기록 거처 = **spec 문서 1줄 단일**, spec 부재 시 최초 작성 시 포함 | 거처 단일(거울면 금지), 하한상 spec은 반드시 생김 |
| D10 | TDD = **유형 ①·② 모두 셀로 재현**(a1·a2 분리, rep 5 하한) | 경계 문구 2종 다 검증돼야 D7이 측정된 것 |
| D11 | **기록은 재스캔을 갈음하지 않는다** — 확인 불가 재개는 기록 유무와 무관하게 재스캔 재판정, 기록 = 감사+과거 판정 판독 원본(2026-08-12 review-loop R3 ESCALATE 사용자 판정) | stale 기록 구멍(재개 사이 동일 파일 위험 신호 유입) 원천 차단 — 스캔이 grep 수준이라 갈음 이득 미미, 리비전 스탬프(기계 장치화·D15 압박) 기각 |
| D12 | **종단 간 비용 AC 신설 안 함** — §6 (a1)(a2)에 관찰 지표(생략 선택·D기록 성립·실수행 단계 수, 합부 아님)만 병기(2026-08-12 review-loop R3 ESCALATE 사용자 판정) | 생략 실행률·잔여 비용은 §7 후속 트랙 몫(측정 예산 소폭 유지) — 경량 라벨 ≠ 생략 실행 갭은 관찰 데이터로 후속 판단 |
| D13 | **5 writing-plans-split 생략**(착수 세션 확정 2026-08-12) — 6은 5 해당없음의 자동 귀결(별도 기록 불요), planless 트랙의 impl ledger 위치 = 이 spec 말미 | 단일 파일 스킬 문면 변경이고 태스크 분해·검증 계획을 §6 TDD 계획이 이미 보유 — plan 문서가 더할 분해 없음. C-10·fp-I17 전례 동형 |

## 7단계 구현 기록 (2026-08-12~13, writing-skills TDD)

- 하네스 = harness-9(로컬 `.remember/harness-9/` — mkfix9·mkprompt9·checkfix9 결정적 소스 + RUN 계약 + 스킬 사본 2종. 픽스처 repo 9종: 경량 베이스 · 파일 표면 위험 · 재개 4종 · 스캔 불명확 3종). 기대 정답 = `.remember/tdd9-plan.md`(물리 분리), 채점 = `.remember/tdd9-scores.md`, 큐 = `.remember/tdd9-batchq.md`. Sonnet 서브에이전트, 셀별 독립 런, 웨이브 ≤16 + 매 웨이브 porcelain 검사.
- **RED (cur 80런)**: 표적 A1(①)·A2(②)·D1(기록 有 재개)·D3(기록 無 재개) **20/20 정식 고착** — "무응답·부재=비승인→정식 기본값" 합리화로 ×26 비용 경로 문면 재현. 대조군 60런 전건 안전(경량 오진입 0). E1은 보류 성향 4/5(→ 문면에 "불명확 일체는 보류가 아니라 정식" 명시 근거).
- **GREEN (new 96런 = 파일럿 16 + 본배치 80)**: **AC1~AC4 전건 충족, REFACTOR 0회** — A1·A2 경량 자동 판정 12/12 + 감사 기록 1줄 서술 전건(신호 ①·② 표기) · B계열 24/24 정식(파일 표면 셀은 실제 판독 근거 — D8 두 입력원) · E계열 18/18 정식(부재·조회 실패·상충 각 fail-closed, 보류 0) · C1 확인 1회 6/6 · C2 정식 유지 6/6(무응답≠확인 불가 명시) · D1·D3 경량 12/12(기록 유무 무관 재스캔) · D2 정식 6/6(stale 기록 맹신 0) · D4 확인 1회 6/6(기록의 질문 우회·재질문 0) · D5 정식 승격 6/6 + 승격 전이 3종 전건(사유 서술·D3/D4 철회·첫 미완 재계산) · 전 런 "파일 수정: 없음"(G-g).
- 유효 176런 · **폐기 8런(정직 기록)**: 스모크 3(픽스처 5번째 오탈자 결함) · E2 cur 4(chmod 000을 `git show`로 우회 가능 — git 미추적 로컬 설정 파일로 교체, checkfix에 우회 불가 검사 추가) · E2 new 1(교차 오염 자진 고지 — 이전 rep 출력 열람, 재실행).
- **관찰 지표(D12, §7 후속 트랙 근거)**: A1·A2 12/12가 4·5·6 생략을 선택하고 D기록(D번호+근거) 계획을 서술 — 자율 운용에서 경량 라벨과 생략 실행이 동행. 잔여 비용 = 하한 중심 5단계(1–2·3·7·8·9). D3 판정 라인 변량(4↔7, 둘 다 D기록 동반)은 후속 판단 재료.
- **D15**: 개정 총량 98줄(하드 100 이내). 소프트 95 초과 3줄은 압축 후보가 전건 기측정 문면(C-10·fp-I15/16/19 GREEN 근거)이라 "이식 항목 비압축" 준용으로 유지.
- **릴리스 표면(AC5)**: SKILL.md 본반영 + plugin.json 0.17.0 범프(선점 재확인 — 미선점) + README 3종(en/ko/ja) 경량 승인 문단 부분 대체(이 커밋). 3머신 `/plugin update` 안내 = 핸드오프 기재. 8단계 게이트 = 이 TDD GREEN 기록 갈음(npm 스크립트 없는 repo 규정).

## 적대검증 ledger (spec)

- 루프: review-loop(spec) 2026-08-12 시작. base = `38c1859`(해소 SHA). 예산: max 5 · confirm 2 · auto 3.
- score 이력(산식: 미판정 blocking 가중합 c4/h3/m1, §2c 스냅샷 시점, 미확인 FIXED 큐 제외): R1 = 4 (high 1 · medium 1) → R2 = 7 (high 2 · medium 1, 전건 신규) → R3 = 6 (high 2, 전건 신규 — 전건 ESCALATE).
- R3 종료 시점: 적대 소진 3/5 (== auto-rounds → batch flush 수행, ESCALATE 2건 사용자 판정으로 닫힘 → D11·D12). 전환 신호 2(수정 큐 소진) 발화 → 확인 모드 진입.
- C1 확인 라운드(확인 소진 1/2): F1~F5·F7 **소멸 확인 6건** · F6 재분류 medium(정본 D4 근거 셀에 구 의미론 "가드 3 재확인 대체" 잔존 — D11 충돌) → FIXED(`aa39a60`) · 판정 감사 대상 없음 · 신규 finding 없음 · verdict **fail**(D4 정정 조건부) → **적대 복귀 1회 발동**(상한 밖).
- 복귀 적대 라운드(상한 밖 — 카운터 불변, score 스냅샷 = 6, high 2 신규): RB-1·RB-2 FIXED 후보 분류 → 일시중단 후 동일 세션 재개, `e4d6ce4`로 수정.
- C2 재진입 확인(예약분 — 확인 소진 불변 1/2): **F6′·RB-1·RB-2 전건 소멸 확인** · 회귀 없음 · 판정 감사 대상 없음 · 신규 0 · verdict **PASS** → **성공 종료**(미판정 blocking 0 · 미확인 FIXED 큐 0 · verdict pass). 루프 총계: 적대 3 + 복귀 적대 1, 확인 1 + 재진입 1. disposition: FIXED 9(사용자 판정 경유 2) · 기타 0 · low 0.

| fingerprint | severity | disposition | 근거 |
|---|---|---|---|
| `spec.md` · 재개 시 감사 기록 유효성 검증 없이 경량 재사용 · 기록 유효 범위·재스캔 규칙 명시 | high | FIXED (R1, `d307b56`) | §3a 기록 유효 범위 신설(동일 요청 한정·불완전=부재·절대 규칙 상시 우선) + §3b 가드 3 소비 규칙 + AC4 범위 확대 픽스처 + 셀 (d) 확장. 소멸 확인(C1) |
| `spec.md` · 스캔 불명확 fail-closed·유형 ② 대조군이 TDD 행렬에 없음 · fail-closed 셀 추가 및 유형 분리 | medium | FIXED (R1, `d307b56`) | AC2 유형 ①·② 분리 + 스캔 불명확 픽스처 명시 + §6 셀 (b1)(b2)(e) 추가. 소멸 확인(C1) |
| `spec.md` · 감사 기록 쓸 주체 부재로 D4와 G-g(읽기 전용 지도)를 동시 만족 불가 · 기록 주체·영속화 계약 명시 | high | FIXED (R2, `15160eb`) | §3a 기록 주체 신설 — 진입 세션이 기록(승격 사유 관례 동형), 지도 판별 무수정, 유실은 fail-closed 재적용 + AC5 무수정 검증 추가. 소멸 확인(C1) |
| `spec.md` · 기록이 소비 세션의 확인 불가 여부까지 영속 우회(대화형 재개에서 확인 생략) · 소비 전제 명문화 | high | FIXED (R2, `15160eb`) | §3a 소비 전제 신설 — 재개 세션도 신호 ①·② 성립 시만 기록 판독, 대화형 재개는 확인 1회(존속 규칙 집행) + AC4·셀 (d) 대화 가능 재개 픽스처. 소멸 확인(C1) |
| `spec.md` · 0.17.0 릴리스·README 3종 동기화가 범위·완료 게이트에 없음 · 릴리스 표면을 범위·AC에 연결 | medium | FIXED (R2, `15160eb`) | §2 릴리스 표면 문단 신설(D5·G-f 운용화 — 범프·README 경량 문단 동기화·3머신 안내) + AC5 게이트 연결. 소멸 확인(C1) |
| `spec.md` · 동일 범위의 변경된 내용에도 오래된 경량 판정 재사용(stale 기록) · 기준 리비전 기록 또는 재스캔 강제 | high | ESCALATE(batch) → 사용자 판정 **FIXED** (R3, `a7faf31`) | **D11 신설** — 재스캔 갈음 폐지(확인 불가 재개는 기록 무관 전건 재스캔), 기록 = 감사+판독 원본으로 축소. §3a 통합 재작성·§3b·AC4·셀 (d) 반영. R1·R2의 소비 의미론 문면을 대체(더 강한 규칙). C1 재분류 medium(D4 근거 셀 구 의미론 잔존) → **FIXED (C1, `aa39a60`)** — D4 근거 셀 D11 정합 정정. 소멸 확인(C2) |
| `spec.md` · 경량 라벨≠생략 실행 — 종단 간 비용 검증 부재로 ×26 잔존 가능 · e2e 비용 AC 추가 | high | ESCALATE(batch) → 사용자 판정 **FIXED** (R3, `a7faf31`) | **D12 신설** — e2e AC 기각, §6 (a1)(a2) 관찰 지표(생략 선택·D기록 성립·실수행 단계 수) 병기 + §7 후속 트랙 연결. 소멸 확인(C1) |
| RB-1 `spec.md` · 스캔 입력원별 대조군 부재(요청 문면에만 위험어 → 파일 표면 안 읽는 구현도 통과) · 입력원별 독립 셀 추가 | high | **FIXED (복귀R, `e4d6ce4`)** | D8 두 입력원 계약의 AC 강제 누락 해소 — AC2를 유형 ①·② × 입력원 2종(요청 문면만/파일·모듈 표면만)으로 분리 + 셀 b계열 분리. 소멸 확인(C2) |
| RB-2 `spec.md` · 재스캔 승격 시 영속 생략 기록 철회·승격 사유 기록·첫 미완 단계 재계산 미검증 · 셀 (d) 합격 조건 확장 | high | **FIXED (복귀R, `e4d6ce4`)** | 즉시 승격 계약의 AC4 연결 — 활성 4·5·6 생략 D기록 재개 픽스처 + 승격 전이 3종(사유 기록·생략 철회·첫 미완 재계산) 합격 조건 + 지도 무수정 유지. 소멸 확인(C2) |

## 적대검증 ledger (impl)

- 루프: review-loop(impl) 2026-08-13 시작(D13 planless — ledger 위치 = 이 spec 말미). base = `ff23db2`(해소 SHA `ff23db25402ab5bcd751c9aa0ab6663a359fa94b`, 인계 지정 diff 기점). 예산: max 5 · confirm 2 · auto 3. 게이트 = writing-skills TDD GREEN 기록 갈음(npm 스크립트 없는 repo). 보안 크리티컬 아님(접촉 표면 = 스킬 문면·릴리스 표면).
- score 이력(산식: 미판정 blocking 가중합 c4/h3/m1, §2c 스냅샷 시점, 미확인 FIXED 큐 제외): R1 = 3 (high 1) → R2 = 0 (신규 0).
- R2 적대(소진 2/5): 신규 finding 0 · verdict approve · fp-H2-R1 적대 비재출현(R2) — 참고 신호일 뿐 큐 유지(§두 큐). 1차 실행은 codex 사용량 한도로 실패(카운터 미소진), 사용자 지시로 재실행. **전환 신호 2(수정 큐 소진) 발화 → batch 적재분 0건 → 확인 모드 진입.**
- 미확인 FIXED 큐: R1 1건(아래 행 — 소멸 확인 대기).

| fingerprint | severity | disposition | 근거 |
|---|---|---|---|
| `SKILL.md` · 확인 불가 폴백이 가드 1의 외부 승인까지 확장(적용 범위 미명시) · 적용 범위를 세션 상태 확인·가드 3 준용에 한정 명문화 + 회귀 셀 추가 | high | FIXED (R1, `0046fe0`) | RED: 7단계 GREEN 출력 재판정 3/12 위반(A1-r4·A2-r3·A2-r5 — 폴백 근거 무확인 머지 자체 결정). 수정 = 폴백 소절 범위 한정 문장 1개(98줄 유지, D15) + AC6·셀 (f) 신설 + README 3종 동기화. GREEN: fix arm 10/10(감사 기록·경량 진입 유지 + 머지·배포 보류·이월 전건, 경량 회귀 0)·REFACTOR 0·porcelain 전건. 유효 22런·폐기 1런(세션 한도). 채점 = `.remember/tdd9-fix-scores.md`, 기대 정답 사전 등록 = `.remember/tdd9-fix-plan.md` |
